---
phase: 04-first-end-to-end-run
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - src/schema/review.ts
  - src/schema/response.ts
  - src/schema/evaluation.ts
  - src/schema/decision-record.ts
  - test/review-schema.test.ts
  - test/response-schema.test.ts
  - test/evaluation-schema.test.ts
  - test/decision-record-schema.test.ts
autonomous: false
requirements: [REVW-01, REVW-02, REVW-03, RCRD-01, RSLV-01]
user_setup: []

must_haves:
  truths:
    - "A structured review (numbered issues, P1-P3 severity, one question each) validates; a malformed one is rejected with typed errors"
    - "A response carrying verdict=reject-with-reason structurally requires a reason; verdict=refine requires a refinement"
    - "An evaluation-round artifact exposes round, proposedBase, and remainingDisagreements fields readable from disk"
    - "A decision record models resolvedDecisions, openDecisions, per-decision lineage, and a unanimousTally scalar"
  artifacts:
    - path: "src/schema/review.ts"
      provides: "ReviewFrontmatter zod schema (REVW-01)"
      exports: ["ReviewIssue", "ReviewFrontmatter"]
    - path: "src/schema/response.ts"
      provides: "ResponseFrontmatter discriminated union on verdict (REVW-02)"
      exports: ["ResponseFrontmatter"]
    - path: "src/schema/evaluation.ts"
      provides: "EvaluationFrontmatter convergence-signal schema (REVW-03)"
      exports: ["EvaluationFrontmatter"]
    - path: "src/schema/decision-record.ts"
      provides: "DecisionRecordFrontmatter schema (RCRD-01/RSLV-01)"
      exports: ["DecisionRecordFrontmatter"]
  key_links:
    - from: "package.json"
      to: "gray-matter@^4"
      via: "dependency install gated by human-verify checkpoint"
      pattern: "gray-matter"
---

<objective>
Install the one new dependency (gray-matter, behind a package-legitimacy human-verify checkpoint) and define the four zod frontmatter schemas every structured Phase-4 phase validates against. These schemas are the contracts the engine's validation gate (04-03), the convergence loop (04-04), and the decision-record writer (04-05) all build against — defining them first prevents downstream scavenger hunts.

Purpose: REVW-01/02/03 and RCRD-01/RSLV-01 all require system-validated structured output; the schemas are the validation source of truth.
Output: gray-matter installed; four schema modules + their unit tests, all green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-first-end-to-end-run/04-CONTEXT.md
@.planning/phases/04-first-end-to-end-run/04-RESEARCH.md
@.planning/phases/04-first-end-to-end-run/04-PATTERNS.md

<interfaces>
<!-- The existing schema idiom to copy EXACTLY. Extracted from src/schema/config.ts. -->

From src/schema/config.ts (the discriminated-union + prefault + superRefine patterns):
```typescript
const Agent = z.discriminatedUnion("vendor", [
  z.object({ vendor: z.literal("claude"), ...Base }),
  z.object({ vendor: z.literal("codex"), ...Base }),
  z.object({ vendor: z.literal("gemini"), ...Base }),
]);
// .prefault({}) — NOT .default({}) — for nested defaults (zod v4: .default skips re-parse)
// .superRefine((c, ctx) => ctx.addIssue({ code: "custom", message, path })) for cross-field invariants
export type MarConfig = z.infer<typeof MarConfig>;
```

From src/schema/manifest.ts (nested-entry + additive `.default([])` style — copy for record/lineage arrays):
```typescript
export const ManifestArtifact = z.object({ path: z.string(), agent: z.string(), seq: z.number(), kind: z.string(), createdAt: z.string() });
droppedAgents: z.array(DroppedAgent).default([]),  // additive, forward-compatible
```
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Package-legitimacy checkpoint + install gray-matter</name>
  <read_first>
    - .planning/phases/04-first-end-to-end-run/04-RESEARCH.md (## Package Legitimacy Audit — gray-matter row)
    - package.json (current dependency list, confirm gray-matter absent)
  </read_first>
  <what-built>
    gray-matter@^4 is the ONLY new dependency this phase (D-36/D-45: read YAML frontmatter back out of agent artifacts for validation; the repo's existing toFrontmatter serializer stays the WRITER). RESEARCH audit: 4.0.3, 6.65M weekly downloads, maintainers jonschlinkert/Atlassian, source github.com/jonschlinkert/gray-matter, NO postinstall script, named in this project's own CLAUDE.md recommended stack. slopcheck was unavailable in the research session, so this is gated as [ASSUMED-PENDING-CHECKPOINT] per the Phase-3 xstate precedent.
  </what-built>
  <how-to-verify>
    1. Open https://www.npmjs.com/package/gray-matter — confirm it is the jonschlinkert package, ~6M+ weekly downloads, last publish 4.0.3.
    2. Run `npm view gray-matter scripts.postinstall` — confirm output is empty (no postinstall script).
    3. If satisfied, the executor runs `npm install gray-matter@^4` and `npm view gray-matter version` (expect 4.0.3).
  </how-to-verify>
  <action>This is a blocking-human legitimacy gate — it is NOT auto-approvable regardless of auto_advance. After human approval, install `gray-matter@^4` via npm, leaving the existing injection-safe `toFrontmatter`/`yamlScalar` serializer in artifacts.ts as the sole WRITER (gray-matter is parse-only). Do not import gray-matter anywhere in this plan — schemas only consume already-parsed `data` objects.</action>
  <verify>
    <automated>node -e "require('gray-matter'); console.log(require('gray-matter/package.json').version)"</automated>
  </verify>
  <acceptance_criteria>
    - `gray-matter` appears in package.json dependencies at `^4`
    - `node -e "require('gray-matter')"` exits 0
    - artifacts.ts `toFrontmatter`/`yamlScalar` is unchanged (still the writer)
  </acceptance_criteria>
  <resume-signal>Type "approved" to install gray-matter, or describe concerns</resume-signal>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Review + Response zod schemas (REVW-01, REVW-02)</name>
  <files>src/schema/review.ts, src/schema/response.ts, test/review-schema.test.ts, test/response-schema.test.ts</files>
  <read_first>
    - src/schema/config.ts (the exact module shape: import z, named const schemas, discriminatedUnion, superRefine, z.infer exports)
    - test/config.test.ts (the schema parse/reject test structure to copy)
    - .planning/phases/04-first-end-to-end-run/04-RESEARCH.md (## Code Examples — ReviewFrontmatter and ResponseFrontmatter)
    - .planning/phases/04-first-end-to-end-run/04-PATTERNS.md (review.ts / response.ts pattern assignments)
  </read_first>
  <behavior>
    - review valid: { phase:"review", author:"codex", targets:"claude", issues:[{n:1, severity:"P1", question:"..."}] } → safeParse ok
    - review rejected: severity outside P1|P2|P3 → safeParse fail; empty issues array → fail; missing question → fail
    - review rejected: duplicate issue `n` values → fail (superRefine, mirror config.ts duplicate-name check)
    - response valid: verdict:"accept" with issueRef → ok
    - response rejected: verdict:"reject-with-reason" WITHOUT reason → fail; verdict:"refine" WITHOUT refinement → fail (discriminated union enforces it)
  </behavior>
  <action>Create `src/schema/review.ts`: `ReviewIssue` = z.object with `n` (z.number().int().positive()), `severity` (z.enum(["P1","P2","P3"])), `question` (z.string().min(1)); `ReviewFrontmatter` = z.object with `phase` z.literal("review"), `author` z.string().min(1), `targets` z.string().min(1) (which peer draft this review critiques — Pattern 4 routing key), `issues` z.array(ReviewIssue).min(1); add a `.superRefine` that flags duplicate `issues[].n` values exactly as config.ts flags duplicate agent names (code:"custom", path:["issues"]); export `type ReviewFrontmatter = z.infer<...>`. Create `src/schema/response.ts`: a `Verdict` = z.discriminatedUnion("verdict", [...]) with three variants — `accept` ({issueRef:int positive}), `reject-with-reason` ({issueRef, reason:string min 1}), `refine` ({issueRef, refinement:string min 1}); `ResponseFrontmatter` = z.object with `phase` z.literal("response"), `author` z.string().min(1), `reviewOf` z.string().min(1) (which review artifact this answers), `responses` z.array(Verdict).min(1); export the inferred type. Write both test files mirroring test/config.test.ts: cover each behavior case above.</action>
  <verify>
    <automated>npx vitest run test/review-schema.test.ts test/response-schema.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run test/review-schema.test.ts test/response-schema.test.ts` passes
    - `src/schema/review.ts` exports `ReviewIssue`, `ReviewFrontmatter`, and the `ReviewFrontmatter` type
    - `src/schema/response.ts` exports `ResponseFrontmatter` and discriminates on `verdict`
    - A response with `verdict:"reject-with-reason"` and no `reason` field fails safeParse (asserted in test)
    - `npx tsc --noEmit` clean for both new modules
  </acceptance_criteria>
  <done>Review and response frontmatter schemas validate well-formed structured artifacts and reject malformed ones, with typed errors available for the D-38 retry; tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Evaluation + Decision-Record zod schemas (REVW-03, RCRD-01, RSLV-01)</name>
  <files>src/schema/evaluation.ts, src/schema/decision-record.ts, test/evaluation-schema.test.ts, test/decision-record-schema.test.ts</files>
  <read_first>
    - src/schema/manifest.ts (nested-entry + container + .default([]) additive style to copy)
    - src/schema/config.ts (z.infer export idiom)
    - .planning/phases/04-first-end-to-end-run/04-RESEARCH.md (Pattern 5 agreement signal A3; Pattern 6 decision-record fields)
    - .planning/phases/04-first-end-to-end-run/04-PATTERNS.md (evaluation.ts / decision-record.ts pattern assignments)
  </read_first>
  <behavior>
    - evaluation valid: { phase:"evaluation", author, round:1, proposedBase:"claude", remainingDisagreements:[], citations:[...] } → ok
    - evaluation rejected: round:0 or negative → fail; missing proposedBase → fail
    - decision-record valid: { runId, resolvedDecisions:[{id, summary, rationale, lineage:[...]}], openDecisions:[], unanimousTally:0 } → ok
    - decision-record valid with defaults: omitting resolvedDecisions/openDecisions defaults to [] (additive forward-compat like droppedAgents)
    - decision-record rejected: a resolvedDecision missing rationale → fail
  </behavior>
  <action>Create `src/schema/evaluation.ts`: `EvaluationFrontmatter` = z.object with `phase` z.literal("evaluation"), `author` z.string().min(1), `round` z.number().int().positive() (Pitfall 3 round disambiguation), `proposedBase` z.string().min(1) (the agent whose draft this evaluator proposes as base — the agreement-detection signal A3), `remainingDisagreements` z.array(z.string()) (empty/conceded ⇒ convergence), `citations` z.array(z.string()).default([]) (cited evidence refs to peer artifacts, REVW-03). Create `src/schema/decision-record.ts`: a `LineageRef` = z.string().min(1) (artifact ref like "002-codex-review.md issue 3"); a `ResolvedDecision` = z.object({ id: z.string().min(1), summary: z.string().min(1), rationale: z.string().min(1), lineage: z.array(LineageRef).default([]) }) (D-46 contested-only, D-47 per-decision lineage); an `OpenDecision` = z.object({ id, summary, reason: z.string().min(1) }) (escalations, D-42); `DecisionRecordFrontmatter` = z.object with `runId` z.string().min(1), `resolvedDecisions` z.array(ResolvedDecision).default([]), `openDecisions` z.array(OpenDecision).default([]), `unanimousTally` z.number().int().min(0).default(0) (the non-contested one-line tally, D-46), and a compact `runChain` z.array(z.string()).default([]) (input → base → final, D-47). Export inferred types from both. Write both test files mirroring test/manifest.test.ts.</action>
  <verify>
    <automated>npx vitest run test/evaluation-schema.test.ts test/decision-record-schema.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run test/evaluation-schema.test.ts test/decision-record-schema.test.ts` passes
    - `src/schema/evaluation.ts` exports `EvaluationFrontmatter` with `round`, `proposedBase`, `remainingDisagreements`
    - `src/schema/decision-record.ts` exports `DecisionRecordFrontmatter` with `resolvedDecisions`, `openDecisions`, `unanimousTally`
    - Omitting `resolvedDecisions` parses (defaults to `[]`) — asserted in test
    - A `resolvedDecisions` entry missing `rationale` fails safeParse — asserted in test
  </acceptance_criteria>
  <done>Evaluation and decision-record schemas model the convergence signal and the contested-only record; tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → local install | gray-matter package crosses an untrusted supply-chain boundary |
| agent CLI output → orchestrator | structured frontmatter is attacker-influenceable (untrusted shape) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-SC | Tampering | gray-matter install (supply chain) | mitigate | Task 1 blocking-human legitimacy checkpoint; verify npm provenance + no postinstall before install (never auto-approved) |
| T-04-01 | Tampering | agent frontmatter shape | mitigate | zod `safeParse` on every schema; never trust CLI output shape (V5 Input Validation); typed errors feed the D-38 retry downstream |
| T-04-02 | Elevation of Privilege | YAML deserialization (downstream gray-matter parse) | accept | gray-matter defaults to js-yaml SAFE load (no `!!js/function`); confirmed no custom unsafe schema is passed when reads are wired in 04-03; schemas here never call gray-matter directly |
</threat_model>

<verification>
- `npx vitest run test/review-schema.test.ts test/response-schema.test.ts test/evaluation-schema.test.ts test/decision-record-schema.test.ts` all green
- `npx tsc --noEmit` clean
- `node -e "require('gray-matter')"` exits 0
</verification>

<success_criteria>
- gray-matter@^4 installed behind a verified human checkpoint
- Four schema modules exist with exported types and full unit coverage of valid/invalid cases
- No schema imports gray-matter (schemas validate already-parsed objects)
</success_criteria>

<output>
Create `.planning/phases/04-first-end-to-end-run/04-01-SUMMARY.md` when done
</output>
