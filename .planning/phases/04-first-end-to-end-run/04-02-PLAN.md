---
phase: 04-first-end-to-end-run
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/templates/agent-instructions.md.tmpl
  - src/protocol/instructions.ts
  - src/workspace/scope.ts
  - test/instructions.test.ts
autonomous: false
requirements: [REVW-01, REVW-02]
user_setup: []

must_haves:
  truths:
    - "Each agent's scoped working folder is seeded with its vendor-native instruction file rendered from ONE source-of-truth template (CLAUDE.md→claude, AGENTS.md→codex, GEMINI.md→gemini)"
    - "An agent invoked inside its scoped cwd does NOT inherit this repo's root CLAUDE.md (GSD workflow directives) — the seeded format contract is the only instruction file in effect"
    - "The format contract specifies numbered issues with P1-P3 severity and one concrete question each, and per-issue verdicts accept/reject-with-reason/refine"
  artifacts:
    - path: "src/templates/agent-instructions.md.tmpl"
      provides: "Single source-of-truth format contract (D-36/D-37)"
      min_lines: 25
    - path: "src/protocol/instructions.ts"
      provides: "seedInstructions: render template → per-vendor file + ancestor-inheritance neutralization"
      exports: ["seedInstructions"]
  key_links:
    - from: "src/workspace/scope.ts"
      to: "src/protocol/instructions.ts"
      via: "scopedWorkdir calls seedInstructions after the input.md copy"
      pattern: "seedInstructions"
---

<objective>
Settle the phase's single highest-risk discovery (Pitfall 1: project-root instruction-file inheritance) in a Wave-0 empirical spike, then deliver the format-contract delivery mechanism (D-37): one source-of-truth template rendered into per-vendor instruction files seeded into each agent's scoped cwd. This is load-bearing for REVW-01/02 — if the seeded contract is diluted by this repo's own root CLAUDE.md, every structured phase downstream produces unvalidatable output.

Purpose: the format contract MUST reach each agent reliably and MUST NOT be overridden by ancestor instruction files; this must be proven before the live 3-vendor checkpoint (04-05).
Output: the template, the renderer/seeder wired into scope.ts, and a spike test that proves neutralization empirically.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-first-end-to-end-run/04-CONTEXT.md
@.planning/phases/04-first-end-to-end-run/04-RESEARCH.md
@.planning/phases/04-first-end-to-end-run/04-PATTERNS.md
@docs-case-study.md

<interfaces>
<!-- The scope.ts seam to extend. Extracted from src/workspace/scope.ts. -->

From src/workspace/scope.ts:
```typescript
const AGENT_NAME_RE = /^[A-Za-z0-9_-]+$/;        // reuse via assertSafeAgent; do not re-implement
function assertSafeAgent(agent: string): void     // charset gate (path-traversal containment)
export async function scopedWorkdir(runDir: string, agent: string, inputPath: string): Promise<string> {
  const dir = join(runDir, "work", agent);
  await ensureDir(dir);
  await copy(inputPath, join(dir, "input.md"));    // ← seedInstructions is called right after this
  return dir;
}
```

scopedWorkdir is invoked from engine.ts:116 for the draft phase only today. The vendor of the
agent is available at that call site (entry.vendor) but is NOT currently passed into scopedWorkdir.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Format-contract template + per-vendor renderer (D-36/D-37)</name>
  <files>src/templates/agent-instructions.md.tmpl, src/protocol/instructions.ts</files>
  <read_first>
    - docs-case-study.md (behavioral spec: the structured review style, integrator behavior, anti-patterns — source the contract's wording from here)
    - .planning/phases/04-first-end-to-end-run/04-CONTEXT.md (D-36/D-37 exact format requirements)
    - src/workspace/scope.ts (assertSafeAgent + scopedWorkdir seam to copy and reuse)
    - .planning/phases/04-first-end-to-end-run/04-PATTERNS.md (instructions.ts pattern assignment; No Analog Found row for the template)
  </read_first>
  <action>Create `src/templates/agent-instructions.md.tmpl` — the SINGLE source of truth for the format contract, identical content rendered to all three vendors (D-37: no per-vendor format divergence). It must specify: (1) REVIEW output = markdown with YAML frontmatter; frontmatter carries `phase: review`, `author`, `targets` (the peer being reviewed), and `issues` as a numbered list where each issue has `n`, `severity` (one of P1, P2, P3), and exactly one concrete `question`; (2) RESPONSE output = frontmatter with `phase: response`, `author`, `reviewOf`, and `responses` where each carries a per-issue `verdict` of exactly `accept`, `reject-with-reason` (with a `reason`), or `refine` (with a `refinement`), keyed by `issueRef`; (3) EVALUATION output = frontmatter with `round`, `proposedBase`, `remainingDisagreements`, and `citations` to peer artifacts with cited evidence; (4) INTEGRATION output = a per-addition verdict plus the merged document body. Keep prose body human-readable; machine-readable data lives in frontmatter (D-36). Source wording/severity conventions from docs-case-study.md. Create `src/protocol/instructions.ts`: a `VENDOR_FILE` map `{ claude: "CLAUDE.md", codex: "AGENTS.md", gemini: "GEMINI.md" }`; an async `seedInstructions(workdir, vendor)` that reads the template once (resolve the template path relative to the module, e.g. via `new URL("../templates/agent-instructions.md.tmpl", import.meta.url)`, NOT a hardcoded cwd-relative path — the run cwd differs) and writes it verbatim to `join(workdir, VENDOR_FILE[vendor])`. Do not parse or template-substitute beyond identity render (D-37: single source, no divergence).</action>
  <verify>
    <automated>npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `src/templates/agent-instructions.md.tmpl` exists and contains the literal tokens `P1`, `P2`, `P3`, `accept`, `reject-with-reason`, `refine`, `proposedBase`
    - `src/protocol/instructions.ts` exports `seedInstructions` and maps claude→CLAUDE.md, codex→AGENTS.md, gemini→GEMINI.md
    - instructions.ts resolves the template path via `import.meta.url`, not a cwd-relative string (grep: `import.meta.url` present)
    - `npx tsc --noEmit` clean
  </acceptance_criteria>
  <done>One template renders byte-identical into each vendor's native instruction filename; format contract specifies issues/severity/question and verdicts.</done>
</task>

<task type="auto">
  <name>Task 2: Wire seeding into scopedWorkdir + ancestor-inheritance neutralization spike (Pitfall 1)</name>
  <files>src/workspace/scope.ts, test/instructions.test.ts</files>
  <read_first>
    - src/workspace/scope.ts (scopedWorkdir — add the seedInstructions call after the input.md copy; thread vendor through)
    - src/protocol/instructions.ts (seedInstructions signature from Task 1)
    - test/scope-independence.test.ts (the spike/seeding test structure to mirror)
    - .planning/phases/04-first-end-to-end-run/04-RESEARCH.md (Pitfall 1 — the three neutralization options + warning signs)
  </read_first>
  <action>Modify `src/workspace/scope.ts`: change `scopedWorkdir(runDir, agent, inputPath)` to also accept the agent's `vendor` and call `await seedInstructions(dir, vendor)` immediately after the `copy(inputPath, join(dir, "input.md"))` line, so each scoped workdir is seeded with its vendor-native instruction file alongside input.md. Reuse the existing `assertSafeAgent` charset gate for all paths (do not re-implement containment). Then implement the Pitfall-1 neutralization: because all three CLIs walk from this git repo's root down to cwd discovering instruction files, the repo's own root CLAUDE.md (GSD workflow directives) would leak in and override the seeded contract. Choose and implement the neutralization empirically validated by the spike test below — preferred order per RESEARCH: (a) place a stop-marker / ensure no ancestor AGENTS.md/GEMINI.md exists and document the claude `--bare` requirement for the live path, or (b) seed from an out-of-repo temp cwd. Document the chosen mechanism in a code comment naming the vendor flags the live adapter path must pass (claude `--bare`; codex `--ignore-user-config`; gemini config-trust scoping). Create `test/instructions.test.ts` mirroring test/scope-independence.test.ts: (1) assert scopedWorkdir seeds the correct vendor file (CLAUDE.md/AGENTS.md/GEMINI.md) with template content into work/<agent>/; (2) the SPIKE — construct a fixture where an ancestor dir contains a conflicting CLAUDE.md, and assert via the chosen mechanism that the seeded file is the effective/nearest contract (e.g. assert the seeded file exists and the neutralization marker/flag is in place); the test must FAIL if no neutralization is applied. Keep the test hermetic (filesystem only; no live CLI).</action>
  <verify>
    <automated>npx vitest run test/instructions.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run test/instructions.test.ts` passes
    - scopedWorkdir seeds `work/<agent>/CLAUDE.md` (or AGENTS.md/GEMINI.md per vendor) with the template content — asserted by reading the seeded file back
    - The neutralization spike test asserts the seeded contract is effective despite a planted ancestor instruction file, and fails when neutralization is removed
    - scope.ts reuses `assertSafeAgent` (grep: `assertSafeAgent` present in the new path)
    - `npx vitest run test/scope-independence.test.ts` still green (no regression of PROT-04)
  </acceptance_criteria>
  <done>Each scoped workdir carries its vendor-native instruction file; the ancestor-inheritance risk is neutralized and proven by a hermetic spike test before the live checkpoint.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo filesystem → agent cwd | ancestor instruction files cross into the agent's discovered config |
| agent name → directory path | agent name becomes a path segment under work/<agent>/ |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-03 | Tampering / Integrity | instruction-file inheritance overriding the format contract | mitigate | Task 2 neutralization (stop-marker / vendor ignore-config flags / out-of-repo cwd) proven by the hermetic spike test BEFORE the live checkpoint (load-bearing for REVW-01) |
| T-04-04 | Tampering | agent-name path traversal in seeded dirs | mitigate | reuse existing `assertSafeAgent` charset gate; all paths via `join(workdir, ...)` containment |
| T-04-05 | Tampering | input document influencing review content (prompt injection) | accept | out of Phase 4 scope (TEST docs only); STATE.md flags untrusted legal inputs for Phase 5 — do not over-build here |
</threat_model>

<verification>
- `npx vitest run test/instructions.test.ts` green
- `npx vitest run test/scope-independence.test.ts` green (no PROT-04 regression)
- `npx tsc --noEmit` clean
</verification>

<success_criteria>
- One source-of-truth template renders into each vendor's native instruction filename
- Ancestor instruction-file inheritance is neutralized and proven hermetically (Pitfall 1 settled pre-live-checkpoint)
- scopedWorkdir seeds the contract into every agent's scoped cwd
</success_criteria>

<output>
Create `.planning/phases/04-first-end-to-end-run/04-02-SUMMARY.md` when done
</output>
