---
phase: 04-first-end-to-end-run
plan: 03
type: execute
wave: 2
depends_on: ["04-01", "04-02"]
files_modified:
  - src/protocol/phases.ts
  - src/protocol/gate.ts
  - src/schema/manifest.ts
  - src/protocol/engine.ts
  - test/fixtures/fake-claude.mjs
  - test/fixtures/fake-codex.mjs
  - test/fixtures/fake-gemini.mjs
  - test/validation-retry.test.ts
  - test/protocol-gate.test.ts
autonomous: true
requirements: [REVW-01, REVW-02, REVW-04]
user_setup: []

must_haves:
  truths:
    - "Each structured phase delivers a thin prompt (references the seeded instruction file) instead of the Phase-3 placeholder"
    - "After a turn writes its artifact, the orchestrator parses the frontmatter and validates it; a malformed artifact triggers exactly ONE retry with the validation errors appended, then a second failure is a failed turn (D-30 skip-failed)"
    - "The integration phase expects exactly ONE writer (the integrator), not the whole roster"
    - "Fixtures emit schema-valid structured review/response/evaluation/integration frontmatter so a hermetic run yields validatable artifacts; a malformed-mode fixture exercises the retry"
  artifacts:
    - path: "src/protocol/phases.ts"
      provides: "Phase descriptor extended with prompt + validate + integrator participants"
      contains: "validate"
    - path: "src/protocol/engine.ts"
      provides: "real prompts + validation-with-one-retry gate + integrator-only fan-out"
      contains: "validation"
  key_links:
    - from: "src/protocol/engine.ts"
      to: "src/schema/review.ts"
      via: "gray-matter parse + ReviewFrontmatter.safeParse after each turn"
      pattern: "safeParse"
    - from: "src/protocol/gate.ts"
      to: "phase.participants"
      via: "expectedParticipantCount returns 1 for integrator"
      pattern: "integrator"
---

<objective>
Turn the Phase-3 skeleton (placeholder prompts, all-mode fan-out, no content validation) into the structured-phase core: thin per-phase prompts that reference the seeded instruction file, a validation-with-one-retry gate that validates each turn's frontmatter against the 04-01 schemas, the integrator-only participant branch wired into the gate, and additive manifest kinds. This is the vertical slice that makes a hermetic run produce SYSTEM-VALIDATED structured reviews and responses (REVW-01/02) and prepares the single-integrator merge gate (REVW-04).

Purpose: REVW-01/02 require system-validated structured output; REVW-04 requires exactly one integrator writer. Both ride existing seams (engine.ts:110 placeholder, gate.ts:53 stub, phases.ts descriptor).
Output: real prompts + validation gate + integrator gate branch + fixtures that emit valid/malformed structured content; validation-retry and gate tests green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-first-end-to-end-run/04-CONTEXT.md
@.planning/phases/04-first-end-to-end-run/04-RESEARCH.md
@.planning/phases/04-first-end-to-end-run/04-PATTERNS.md
@.planning/phases/03-protocol-engine-independence-enforcement/03-02-SUMMARY.md

<interfaces>
<!-- The seams being extended. Extracted from the live codebase. -->

From src/protocol/phases.ts (extend this frozen descriptor — keep `as const`):
```typescript
export interface Phase {
  readonly name: "draft"|"review"|"response"|"evaluation"|"integration"|"validation";
  readonly kind: string;
  readonly scoped: boolean;
  readonly participants: "all" | "integrator";   // integration flips to "integrator" (REVW-04)
}
```

From src/protocol/gate.ts (the documented stub at lines 53-57 — implement the integrator branch):
```typescript
export function expectedParticipantCount(phase: Phase, roster: AgentEntry[]): number {
  if (phase.participants === "all") return roster.length;
  return roster.length;  // ← replace with: if (participants === "integrator") return 1;
}
```

From src/protocol/engine.ts:110 (the placeholder to replace) and the turn seam:
```typescript
const promptText = `phase: ${phase.name}\ninput: ${inputPath}`;   // ← replace with phase.prompt(ctx)
const turn = await withRetry(() => adapter.invoke({...}), { retries, classify, onAttempt });
// validation-with-one-retry wraps the turn AFTER withRetry (Pitfall 5: distinct from transport retry)
```

From src/workspace/artifacts.ts (the WRITER stays; gray-matter is the READER):
```typescript
function toFrontmatter(fields): string   // injection-safe serializer — UNCHANGED, still writes
export function writeArtifact(runDir, seq, agent, opts): Promise<{path, rawPath}>
```

From src/config.ts (zod-issue formatting to copy for the retry-error string):
```typescript
err.issues.map((i) => `  ${i.path.length ? i.path.join(".") : "(root)"}: ${i.message}`).join("\n")
```

From test/fixtures/fake-claude.mjs (the `--emit <kind>` mode to extend toward valid structured frontmatter).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend Phase descriptor + gate integrator branch + manifest kinds (REVW-04)</name>
  <files>src/protocol/phases.ts, src/protocol/gate.ts, src/schema/manifest.ts, test/protocol-gate.test.ts</files>
  <read_first>
    - src/protocol/phases.ts (the frozen PHASES table + Phase interface to extend)
    - src/protocol/gate.ts (expectedParticipantCount stub at lines 53-57; requiredArtifactsExist unchanged)
    - src/schema/manifest.ts (ManifestArtifact.kind is z.string already; status enum to extend)
    - test/protocol-gate.test.ts (existing gate unit tests to extend with the integrator branch)
    - src/schema/review.ts, src/schema/response.ts, src/schema/evaluation.ts (from 04-01 — the validate functions reference these)
  </read_first>
  <action>Extend the `Phase` interface in `src/protocol/phases.ts` with two fields: `prompt: (ctx: PhasePromptCtx) => string` (thin per-phase prompt, D-37) and `validate?: (frontmatter: unknown) => { ok: true } | { ok: false; errors: string }` (optional zod gate). Define a `PhasePromptCtx` type carrying what the prompt needs: `{ inputPath: string; phaseName: string }` (keep thin — the format contract lives in the instruction file, NOT the prompt). Populate each PHASES entry: draft/review/response/evaluation/integration/validation each get a thin `prompt` returning e.g. `"review the documents in this folder per your instructions"` (no format-stuffing — Anti-Pattern); attach `validate` to review (ReviewFrontmatter.safeParse), response (ResponseFrontmatter), evaluation (EvaluationFrontmatter), integration (a per-addition-verdict check — reuse ResponseFrontmatter's verdict shape or a minimal integration schema if needed); draft/validation may omit validate. Flip the integration entry to `participants: "integrator"`. Implement the integrator branch in `gate.ts` `expectedParticipantCount`: `if (phase.participants === "integrator") return 1;` (REVW-04 single writer). In `src/schema/manifest.ts`, the `kind` field is already `z.string()` so evaluation/decision-record kinds parse unchanged — additively extend the `status` enum to include `"escalated"` (O-2 fallback-base outcome; mirror the optional/additive `droppedAgents` precedent). Extend test/protocol-gate.test.ts to assert `expectedParticipantCount` returns 1 for a `participants:"integrator"` phase and roster.length for `"all"`.</action>
  <verify>
    <automated>npx vitest run test/protocol-gate.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `phases.ts` `Phase` has `prompt` and optional `validate`; PHASES integration entry has `participants:"integrator"`
    - `gate.ts` `expectedParticipantCount` returns 1 for the integrator branch — asserted in test/protocol-gate.test.ts
    - manifest `status` enum includes `"escalated"`; `kind` still accepts `"evaluation"`/`"decision-record"`
    - prompts are thin: grep phases.ts shows NO `P1`/`severity`/`accept`/`reject-with-reason` tokens in prompt strings (format lives in the instruction file)
    - `npx vitest run test/protocol-gate.test.ts` passes; `npx tsc --noEmit` clean
  </acceptance_criteria>
  <done>The phase descriptor carries per-phase prompts and validators, the gate expects one integrator, and the manifest accepts the new kinds/status — all without format-stuffing prompts.</done>
</task>

<task type="auto">
  <name>Task 2: Real prompts + validation-with-one-retry gate + integrator-only fan-out (REVW-01/02/04, D-38)</name>
  <files>src/protocol/engine.ts</files>
  <read_first>
    - src/protocol/engine.ts (runPhase fan-out at lines 60-193; placeholder prompt line 110; withRetry seam 119-142; manifest-sequential-append 164-191; applySkipFailed path 241-269)
    - src/workspace/artifacts.ts (writeArtifact — written.path is the abs path to gray-matter-read for validation)
    - src/schema/review.ts, src/schema/response.ts, src/schema/evaluation.ts (the validate targets)
    - .planning/phases/04-first-end-to-end-run/04-RESEARCH.md (Pattern 2 validation-with-one-retry; Pitfall 5 transport-vs-validation; Pitfall 2 manifest race; Pitfall 4 integrator gate)
    - .planning/phases/04-first-end-to-end-run/04-PATTERNS.md (engine.ts modified pattern)
  </read_first>
  <action>In `src/protocol/engine.ts`: (1) Replace the placeholder `promptText` at line 110 with `phase.prompt({ inputPath, phaseName: phase.name })` (thin, references the seeded instruction file). (2) Pass each agent's `entry.vendor` into `scopedWorkdir` (now requires vendor per 04-02) so instruction files seed correctly. (3) Implement the validation-with-one-retry gate (D-38) AFTER `withRetry` returns a successful turn — NOT inside it (Pitfall 5: transport retry and validation retry are distinct, do not conflate with the D-23 transport `retries:2`): when `phase.validate` is defined and the turn is ok, read the just-written artifact's `.md` with gray-matter (`matter(readFileSync(written.path,"utf8"))`), run `phase.validate(data)`; on failure re-invoke the SAME adapter ONCE with the prompt plus an appended `## Validation errors to fix\n<formatted zod issues>` block (format issues exactly like config.ts formatIssues), re-write, re-validate; a second failure converts the turn to a FAILED turn (reason `"validation-failed"`) so the existing `applySkipFailed` path drops it (≥2 vendors must survive). Never silently auto-normalize. Keep the gray-matter parse READ-only; the existing `toFrontmatter` serializer still writes. (4) For `participants:"integrator"` phases, fan out over ONLY the designated integrator, not the full surviving roster (the integrator identity is threaded from context — for THIS plan, accept it as a parameter/context field; the convergence loop that SETS it lands in 04-04, so guard with a clear precondition that integration requires a designated integrator). (5) Preserve Pitfall 2: artifact files write concurrently, but `addArtifact` runs SEQUENTIALLY after the settle (do not regress the lines 164-191 discipline). Add a per-validation progress line consistent with the existing `  agent ✓/✗` format (e.g. `  agent ↻ revalidating` on the retry).</action>
  <verify>
    <automated>npx vitest run test/validation-retry.test.ts test/protocol-gate.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - engine.ts no longer contains the literal placeholder `phase: ${phase.name}\ninput: ${inputPath}` (grep absent); uses `phase.prompt(`
    - A turn whose first artifact fails `phase.validate` triggers exactly ONE re-invocation with appended validation errors; a second failure yields a turn with `ok:false` and reason `validation-failed` — asserted in test/validation-retry.test.ts
    - gray-matter is used READ-only (grep: no new call to gray-matter `stringify`); `toFrontmatter` still writes
    - integrator-mode phases fan out over exactly one agent (not roster.length)
    - manifest `addArtifact` calls remain sequential after `allSettled` (no concurrent manifest writes — Pitfall 2 not regressed)
    - `npx vitest run test/validation-retry.test.ts` passes; `npx tsc --noEmit` clean
  </acceptance_criteria>
  <done>Structured phases run real thin prompts, validate each turn's frontmatter with one error-fed retry, fail-closed on a second failure, and the integration phase fans out over only the integrator.</done>
</task>

<task type="auto">
  <name>Task 3: Extend fixtures to emit valid + malformed structured frontmatter (D-49 hermetic)</name>
  <files>test/fixtures/fake-claude.mjs, test/fixtures/fake-codex.mjs, test/fixtures/fake-gemini.mjs</files>
  <read_first>
    - test/fixtures/fake-claude.mjs (the `--emit <kind>` mode at lines 28-32, 60-74 to extend)
    - test/fixtures/fake-codex.mjs, test/fixtures/fake-gemini.mjs (keep all three in lock-step)
    - src/schema/review.ts, src/schema/response.ts, src/schema/evaluation.ts (the shapes the emitted frontmatter must satisfy)
  </read_first>
  <action>Extend each fake-CLI fixture's `--emit <kind>` mode so that for kinds `review`, `response`, `evaluation`, and `integration` it emits a SCHEMA-VALID markdown-plus-frontmatter body (matching the 04-01 schemas) inside the existing vendor success envelope — e.g. a review body whose frontmatter has `phase: review`, `author`, `targets`, and at least one issue `{n,severity:"P1",question}`; a response with at least one accept/reject-with-reason/refine verdict; an evaluation with `round`, `proposedBase`, `remainingDisagreements: []` (so a hermetic run converges); an integration with a per-addition verdict + merged body. Add a NEW mode flag `--emit-malformed <kind>` that emits frontmatter VIOLATING the schema (e.g. severity "P9", or reject-with-reason missing reason) so test/validation-retry.test.ts can drive the D-38 one-retry path. Keep the existing default/happy/planted/bad-json/hang modes unchanged (additive). Make `proposedBase` configurable via an env var (e.g. `MAR_EMIT_BASE`) so a convergence test can steer which agent each fixture proposes as base (used by 04-04). Keep all three fixtures byte-aligned in behavior.</action>
  <verify>
    <automated>node test/fixtures/fake-claude.mjs --emit review | node -e "const m=require('gray-matter');const s=require('fs').readFileSync(0,'utf8');const j=JSON.parse(s);console.log(m(j.result).data.phase)"</automated>
  </verify>
  <acceptance_criteria>
    - `node test/fixtures/fake-claude.mjs --emit review` emits a success envelope whose `.result` body parses via gray-matter to frontmatter with `phase:"review"` and ≥1 issue with severity in P1|P2|P3
    - `--emit response`, `--emit evaluation`, `--emit integration` each emit schema-valid frontmatter for the respective 04-01 schema
    - `--emit-malformed review` emits frontmatter that fails ReviewFrontmatter.safeParse
    - all three fixtures (claude/codex/gemini) implement the same `--emit`/`--emit-malformed` modes; existing default/planted/bad-json/hang modes unchanged
    - `MAR_EMIT_BASE=<agent> node test/fixtures/fake-claude.mjs --emit evaluation` sets `proposedBase` to that agent
  </acceptance_criteria>
  <done>Hermetic fixtures emit schema-valid structured artifacts for every structured phase, plus a malformed mode to exercise the validation retry — zero credits (D-49).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent CLI stdout → orchestrator | structured frontmatter is attacker-influenceable; validated before trust |
| concurrent fan-out → manifest.json | shared single-file index under concurrent writes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-06 | Tampering | malformed agent frontmatter accepted as valid | mitigate | validation-with-one-retry gate (zod safeParse on the gray-matter-parsed data); second failure → failed turn, never auto-normalized (D-38) |
| T-04-07 | Elevation of Privilege | YAML deserialization RCE via crafted frontmatter | mitigate | gray-matter default js-yaml SAFE load (no `!!js/function`); confirm no custom unsafe schema passed at the read site |
| T-04-08 | Denial of Service | manifest concurrent-write race in new fan-outs | mitigate | preserve Phase-3 rule: artifact files concurrent, `addArtifact` sequential after `allSettled` (Pitfall 2) |
| T-04-09 | Spoofing | a non-integrator agent merging (redundant merge) | mitigate | integration fans out over only the integrator; gate expects exactly 1 writer (REVW-04, Pitfall 4) |
</threat_model>

<verification>
- `npx vitest run test/validation-retry.test.ts test/protocol-gate.test.ts` green
- `npx vitest run` (full suite) green — no Phase-1/2/3 regressions
- `npx tsc --noEmit` clean; `npx biome check` clean
</verification>

<success_criteria>
- Structured phases deliver thin prompts and validate every turn against the 04-01 schemas with one error-fed retry (REVW-01/02)
- The integration phase expects exactly one integrator writer (REVW-04 gate wired)
- Fixtures emit valid + malformed structured content for hermetic, zero-credit verification (D-49)
</success_criteria>

<output>
Create `.planning/phases/04-first-end-to-end-run/04-03-SUMMARY.md` when done
</output>
