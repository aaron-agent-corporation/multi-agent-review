---
phase: 04-first-end-to-end-run
plan: 05
type: execute
wave: 4
depends_on: ["04-04"]
files_modified:
  - src/protocol/decision-record.ts
  - src/protocol/engine.ts
  - test/decision-record.test.ts
  - test/protocol-run.e2e.test.ts
autonomous: false
requirements: [RCRD-01, RSLV-01]
user_setup:
  - service: gemini
    why: "True 3-vendor live run for the phase success bar (D-48)"
    env_vars:
      - name: GEMINI_API_KEY
        source: "Google AI Studio (fallback path only if settings.json OAuth on the paid tier is unavailable)"
    dashboard_config:
      - task: "Authenticate gemini CLI headlessly via settings.json OAuth on the paid tier (primary), or set GEMINI_API_KEY (fallback)"
        location: "gemini CLI settings.json / Google AI Studio"

must_haves:
  truths:
    - "Every run produces a decision-record.md in runs/<id>/ with resolved decisions (rationale), open decisions, and per-decision artifact lineage"
    - "The record captures CONTESTED items only — unanimous accepts collapse to a one-line tally (D-46)"
    - "Each integrator resolution and convergence concession is logged with its rationale (RSLV-01)"
    - "A complete 3-agent hermetic run finishes all 6 phases on a test document and produces a decision record (success criterion #1)"
    - "A true 3-vendor LIVE run produces a decision record, human-verified (D-48)"
  artifacts:
    - path: "src/protocol/decision-record.ts"
      provides: "assemble + write decision-record.md from the artifact trail (RCRD-01)"
      exports: ["writeDecisionRecord"]
    - path: "runs/<id>/decision-record.md"
      provides: "the run's auditable decision record"
      contains: "resolvedDecisions"
  key_links:
    - from: "src/protocol/engine.ts"
      to: "src/protocol/decision-record.ts"
      via: "runProtocol calls writeDecisionRecord at run end (terminal write, like setStatus)"
      pattern: "writeDecisionRecord"
    - from: "src/protocol/decision-record.ts"
      to: "src/workspace/manifest.ts"
      via: "readManifest enumerates artifacts; gray-matter parses contested verdicts"
      pattern: "readManifest"
---

<objective>
Complete the v1 success bar: assemble and write the decision record (RCRD-01) from the artifact trail, wire it as the terminal run step, prove the full 3-agent run produces it hermetically (success criterion #1), and gate the phase behind a true 3-vendor LIVE human-verify checkpoint (D-48). The decision record reads as "what was argued and why it landed this way" — contested items only, each with rationale and artifact lineage.

Purpose: RCRD-01 + RSLV-01 require an auditable record of resolved/open decisions; the phase goal requires one complete run producing a decision record.
Output: decision-record writer + engine terminal wiring + full e2e assertion + live 3-vendor checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-first-end-to-end-run/04-CONTEXT.md
@.planning/phases/04-first-end-to-end-run/04-RESEARCH.md
@.planning/phases/04-first-end-to-end-run/04-PATTERNS.md

<interfaces>
<!-- The terminal-write + parse seams to reuse. Extracted from the codebase. -->

From src/workspace/manifest.ts (terminal-write idiom + enumeration):
```typescript
export async function readManifest(runDir): Promise<Manifest>   // enumerate artifacts
export async function setStatus(runDir, status, failureReason?): Promise<void>  // terminal write pattern
// Manifest.status now includes "escalated" (04-03)
```

From src/workspace/artifacts.ts (the WRITER — reuse for decision-record.md; gray-matter is READ-only):
```typescript
function toFrontmatter(fields): string   // injection-safe serializer — use for WRITING the record
// atomic temp-then-rename discipline (writeArtifact / writeManifestAtomic)
```

From src/schema/decision-record.ts (04-01 — the record shape to assemble):
```typescript
DecisionRecordFrontmatter: { runId, resolvedDecisions: [{id,summary,rationale,lineage[]}], openDecisions: [{id,summary,reason}], unanimousTally, runChain[] }
```

From src/protocol/engine.ts (runProtocol terminal: setStatus off the final snapshot — add writeDecisionRecord alongside):
```typescript
if (snapshot.value === "done") { await setStatus(runDir, "completed"); return 0; }
// context.convergence carries concessions + openDecision; integration artifact carries integrator verdicts
```

From src/schema/response.ts (the verdict shapes whose reject-with-reason/refine entries are the contested items, D-46).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Decision-record writer (RCRD-01, RSLV-01, contested-only)</name>
  <files>src/protocol/decision-record.ts, test/decision-record.test.ts</files>
  <read_first>
    - src/workspace/manifest.ts (readManifest enumeration + setStatus terminal-write + atomic temp-then-rename)
    - src/workspace/artifacts.ts (toFrontmatter injection-safe WRITER; writeArtifact atomic discipline)
    - src/schema/decision-record.ts, src/schema/response.ts, src/schema/evaluation.ts (the shapes to assemble from)
    - .planning/phases/04-first-end-to-end-run/04-RESEARCH.md (Pattern 6 decision-record writer; D-46 contested-only; D-47 lineage)
    - .planning/phases/04-first-end-to-end-run/04-PATTERNS.md (decision-record.ts pattern assignment; Shared Patterns atomic write)
    - test/manifest.test.ts (terminal-writer test structure to mirror)
  </read_first>
  <action>Create `src/protocol/decision-record.ts` exporting an async `writeDecisionRecord(runDir, convergenceResult)`. It (1) reads the manifest (`readManifest`), enumerates artifacts, and parses each with gray-matter (READ-only) + the relevant 04-01 zod schema; (2) collects CONTESTED items only (D-46): response verdicts of `reject-with-reason`/`refine`, convergence concessions (from `convergenceResult.concessions`), and integrator `refine`/`reject-conflicts-with-resolved` judgments — each becomes a `resolvedDecisions[]` entry with an `id`, `summary`, the agent-supplied `rationale`/`reason`, and a `lineage` array of per-decision artifact refs (e.g. "002-codex-review.md issue 3 → 003-claude-response.md → 005-claude-integration.md", D-47); (3) collects escalations (`convergenceResult.openDecision`, D-42) into `openDecisions[]`; (4) counts unanimous accepts into `unanimousTally` (one line, not entries, D-46); (5) builds a compact `runChain` (input → base draft → final, D-47 — no duplicate full lineage graph; the manifest already indexes artifacts); (6) validates the assembled object against `DecisionRecordFrontmatter`, then WRITES `decision-record.md` to `runs/<id>/` using the existing `toFrontmatter` serializer for frontmatter + a human rationale narrative body, via atomic temp-then-rename (reuse the writeArtifact/writeManifestAtomic discipline — never write live in place). Write test/decision-record.test.ts (mirror test/manifest.test.ts): build a fixture artifact trail (one contested reject-with-reason, one integrator reject, several unanimous accepts, one escalation) and assert the record has the contested items as resolvedDecisions WITH rationale, the escalation as an openDecision, the unanimous count in unanimousTally, and per-decision lineage refs present.</action>
  <verify>
    <automated>npx vitest run test/decision-record.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `writeDecisionRecord` writes `runs/<id>/decision-record.md` with YAML frontmatter validating against `DecisionRecordFrontmatter`
    - contested items (reject-with-reason/refine/integrator-reject) appear as `resolvedDecisions` each with a non-empty `rationale` and a `lineage` array — asserted in test
    - unanimous accepts collapse to `unanimousTally` (not individual entries) — asserted in test
    - an escalation appears in `openDecisions` (D-42) — asserted in test
    - the record is written via atomic temp-then-rename using the existing `toFrontmatter` writer (gray-matter READ-only — grep: no gray-matter stringify)
    - `npx vitest run test/decision-record.test.ts` passes; `npx tsc --noEmit` clean
  </acceptance_criteria>
  <done>A contested-only decision record with rationale, open decisions, and per-decision lineage is assembled from the artifact trail and written atomically.</done>
</task>

<task type="auto">
  <name>Task 2: Wire decision record as terminal step + full 3-agent hermetic e2e (success #1)</name>
  <files>src/protocol/engine.ts, test/protocol-run.e2e.test.ts</files>
  <read_first>
    - src/protocol/engine.ts (runProtocol terminal: setStatus off the final snapshot — add writeDecisionRecord; context carries convergence result)
    - src/protocol/decision-record.ts (writeDecisionRecord signature from Task 1)
    - test/protocol-run.e2e.test.ts (the hermetic 2-vendor run harness to extend to 3 vendors + decision-record assertion)
    - test/fixtures/fake-gemini.mjs (the 3rd vendor fixture with --emit modes from 04-03)
    - .planning/phases/04-first-end-to-end-run/04-RESEARCH.md (success criterion #1; D-49 hermetic 3-agent dynamics)
  </read_first>
  <action>In `src/protocol/engine.ts` `runProtocol`, after the machine reaches a terminal state, call `await writeDecisionRecord(runDir, snapshot.context.convergence)` as a terminal step alongside `setStatus` (write the record on BOTH `completed` and `escalated` terminal outcomes so an escalated run still yields its record + open decisions; on a hard `failed`/`timeout` the record may be skipped or written best-effort — choose and document). Ensure the convergence result is on context (from 04-04). Extend `test/protocol-run.e2e.test.ts`: add a THREE-vendor hermetic roster (claude+codex+gemini, each injecting its fake fixture bin with `--emit` modes so the run produces schema-valid structured artifacts across all 6 phases and converges), drive `mar run <input>` end-to-end, and assert: (a) status `completed`; (b) one artifact per surviving agent per structured phase kind; (c) `runs/<id>/decision-record.md` EXISTS and its frontmatter parses against `DecisionRecordFrontmatter` (success criterion #1 — the full 3-agent run produces a decision record). Keep the existing 2-vendor RED-anchor and single-vendor-refusal tests passing (additive). Drive convergence via the `MAR_EMIT_BASE` fixture env so all three agents propose the same base (deterministic agreement) — zero credits (D-49).</action>
  <verify>
    <automated>npx vitest run test/protocol-run.e2e.test.ts && npx vitest run && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `runProtocol` calls `writeDecisionRecord` at run end (grep engine.ts: `writeDecisionRecord`)
    - the 3-vendor hermetic e2e asserts a `decision-record.md` exists and validates against `DecisionRecordFrontmatter`
    - the run completes all 6 phases with status `completed` over a 3-vendor fixture roster (success criterion #1)
    - the existing 2-vendor and single-vendor-refusal e2e cases still pass
    - `npx vitest run` (full suite) green; `npx tsc --noEmit` clean
  </acceptance_criteria>
  <done>The full 3-agent run drives all 6 phases hermetically and produces a validated decision record — the v1 success bar proven on fixtures (D-49).</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: TRUE 3-vendor LIVE run human-verify checkpoint (D-48)</name>
  <read_first>
    - .planning/phases/04-first-end-to-end-run/04-CONTEXT.md (D-48 gemini auth: settings.json OAuth primary, GEMINI_API_KEY fallback)
    - .planning/phases/04-first-end-to-end-run/04-RESEARCH.md (Pitfall 6 gemini auth; Pitfall 1 ancestor inheritance — must be settled per 04-02 before this)
    - src/protocol/instructions.ts (the neutralization mechanism + the vendor flags the live path must pass: claude --bare, codex --ignore-user-config, gemini config-trust scoping)
  </read_first>
  <what-built>
    The full structured 6-phase protocol with convergence, single-integrator merge, and decision record, proven hermetically on 3-vendor fixtures (Task 2). This checkpoint runs it ONCE against three REAL vendor CLIs (claude + codex + gemini) on a small test document to confirm the v1 success bar end-to-end with live models. Pitfall-1 ancestor-inheritance neutralization (04-02) and the live vendor flags must be in effect so the seeded format contract — not this repo's root CLAUDE.md — governs agent output.
  </what-built>
  <how-to-verify>
    1. Ensure gemini headless auth works: PRIMARY — settings.json OAuth on your paid tier; FALLBACK — `export GEMINI_API_KEY=<key>`. Verify with `mar preflight` (all three vendors authenticated + responsive).
    2. Create a small test document (e.g. a one-page proposal) at a path of your choosing.
    3. Run `mar run <test-doc>` with a 3-distinct-vendor `mar.config.json` (claude, codex, gemini — live bins, no fake fixtures).
    4. Confirm: the run completes all 6 phases; `runs/<id>/decision-record.md` exists and reads as resolved/open decisions with rationale + lineage; agent review output uses the P1-P3 issue/severity/question format (NOT GSD-workflow language — the warning sign that ancestor inheritance leaked).
    5. If agent output references "GSD workflow" or ignores the issue/severity format, the neutralization (04-02) failed — STOP and report.
  </how-to-verify>
  <action>This checkpoint cannot be automated (live multi-vendor auth + human judgment of output quality). The executor must NOT fake or skip it. If gemini auth cannot be established after both D-48 paths, report the blocker — the hermetic 3-vendor proof (Task 2) still satisfies D-49, but the live 3-vendor success bar requires this checkpoint. Do not downgrade to 2-vendor without user direction.</action>
  <verify>
    <human-check>User confirms: live 3-vendor `mar run` completed all 6 phases, produced a decision-record.md with rationale/lineage, and agent reviews used the P1-P3 structured format (no ancestor-instruction leakage).</human-check>
  </verify>
  <acceptance_criteria>
    - User confirms a real claude+codex+gemini run completed all 6 phases
    - User confirms `decision-record.md` exists with resolved/open decisions, rationale, and lineage
    - User confirms agent reviews followed the seeded P1-P3 issue format (no GSD-workflow leakage — Pitfall 1 neutralization held live)
  </acceptance_criteria>
  <resume-signal>Type "approved" after the live 3-vendor run, or describe what failed</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| artifact trail → decision record | contested verdicts are agent-authored; parsed + validated before recording |
| live vendor CLIs → run | real model output crosses into the protocol; format contract governs via seeded instruction file |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-14 | Repudiation | a resolved decision missing its rationale/lineage | mitigate | DecisionRecordFrontmatter requires rationale on each resolvedDecision; writer asserts via safeParse before write (RCRD-01/RSLV-01) |
| T-04-15 | Tampering | decision record half-written on crash | mitigate | atomic temp-then-rename (reuse writeArtifact/writeManifestAtomic discipline); never write live in place |
| T-04-16 | Tampering / Integrity | ancestor instruction leakage in the LIVE run overrides the format contract | mitigate | 04-02 neutralization + live vendor flags (claude --bare, codex --ignore-user-config, gemini config-trust); checkpoint step 5 explicitly checks for leakage |
| T-04-17 | Information Disclosure | GEMINI_API_KEY in logs/config | accept | key is vendor-managed env var; existing logger redacts prompt bodies and logs only redacted argv (D-15/D-25); key never written to artifacts |
</threat_model>

<verification>
- `npx vitest run` (full suite, incl. 3-vendor e2e + decision-record) green
- `npx tsc --noEmit` clean; `npx biome check` clean
- Live 3-vendor `mar run` human-verified: 6 phases + decision-record.md + structured P1-P3 format (no leakage)
</verification>

<success_criteria>
- Every run produces a contested-only decision record with rationale, open decisions, and per-decision lineage (RCRD-01/RSLV-01)
- A complete 3-agent hermetic run produces a decision record (success criterion #1, D-49)
- A true 3-vendor LIVE run is human-verified end-to-end (D-48 — the v1 success bar)
</success_criteria>

<output>
Create `.planning/phases/04-first-end-to-end-run/04-05-SUMMARY.md` when done
</output>
