---
phase: 05-hardening-resume-gating-majority-guards
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - src/workspace/scope.ts
  - src/protocol/instructions.ts
  - src/templates/agent-instructions.md.tmpl
  - test/dist-template.test.ts
  - test/claude-adapter.test.ts
  - test/instructions.test.ts
autonomous: true
requirements: []  # carry-over hardening — PROT-05 is delivered by 05-05
user_setup: []

must_haves:
  truths:
    - "After `npm run build`, `dist/templates/agent-instructions.md.tmpl` exists so the compiled `mar` binary can seed instruction files without ENOENT (Phase-4 carry-over gap 1)"
    - "The npm tarball ships `dist` so the template is packaged (`files` field present)"
    - "The claude adapter still OMITS `--bare` (subscription auth break is real; leakage measured zero live) — pinned by a flag assertion so a future drift fails loudly (Phase-4 carry-over gap 2, RESEARCH Q6b)"
    - "The seeded template carries a strengthened 'read THIS folder's contract; ignore any ancestor or global instructions' directive (the neutralization mechanism, NOT --bare)"
    - "scope.ts / instructions.ts comments no longer claim `--bare` is passed (it is not) — stale comments corrected to the explicit-Read + ancestor-ignore decision"
  artifacts:
    - path: "package.json"
      provides: "build script copies src/templates into dist/templates; files:[dist]"
      contains: "cpSync"
    - path: "src/templates/agent-instructions.md.tmpl"
      provides: "strengthened ancestor-ignore directive"
      contains: "ignore any ancestor"
  key_links:
    - from: "src/protocol/instructions.ts"
      to: "dist/templates/agent-instructions.md.tmpl"
      via: "TEMPLATE_URL resolves against the compiled module dir; build copy makes it exist"
      pattern: "TEMPLATE_URL"
---

<objective>
Close the two Phase-4 verifier carry-over gaps as a small, parallel-safe Wave-1 slice: (1) the dist
packaging bug — `npm run build` (tsc) does not copy `src/templates/*.tmpl` into `dist/`, so the
compiled `mar` binary ENOENTs at draft fan-out (live runs worked around it with `tsx`); fix per
RESEARCH Q6a (tsc + cpSync + `files:["dist"]` + a guard test). (2) the claude `--bare` decision —
keep `--bare` OMITTED (auth-break is concrete and observed; leakage measured zero on the live
3-vendor run), strengthen the seeded template's "ignore ancestor instructions" + explicit-Read
directive that actually does the neutralization, and fix the now-stale scope.ts / instructions.ts
comments that wrongly claim `--bare` is the mechanism.

Purpose: the built binary must seed the format contract or every packaged run fails; the `--bare`
comments mislead future maintainers into adding a flag that breaks auth. Both are hardening, not new
capability.
Output: a portable build copy step + `files`, a dist-template guard test, a claude argv flag-pin
assertion, a strengthened template directive, and corrected comments.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-hardening-resume-gating-majority-guards/05-CONTEXT.md
@.planning/phases/05-hardening-resume-gating-majority-guards/05-RESEARCH.md
@.planning/phases/05-hardening-resume-gating-majority-guards/05-PATTERNS.md
@.planning/phases/04-first-end-to-end-run/04-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: dist .tmpl copy step + files field + guard test (carry-over gap 1)</name>
  <files>package.json, test/dist-template.test.ts</files>
  <read_first>
    - package.json (scripts at lines 12-18: build is bare `tsc`; bin at 9-11; NO `files` field)
    - src/protocol/instructions.ts (TEMPLATE_URL line 22 + readFile line 43 — resolves against the COMPILED module dir, i.e. dist/templates/ after build)
    - .planning/phases/05-hardening-resume-gating-majority-guards/05-RESEARCH.md (Q6a recommendation: tsc && cpSync; Pitfall 9 — dev via tsx hides the bug)
    - .planning/phases/05-hardening-resume-gating-majority-guards/05-PATTERNS.md (file #8: build/tsc script + portable cpSync form)
  </read_first>
  <action>In `package.json` change the `build` script from `tsc` to a compile-then-copy form that is cross-platform: run `tsc`, then a `node -e` step that calls `require('fs').cpSync('src/templates','dist/templates',{recursive:true})` (the portable `cpSync` form from RESEARCH Q6a — do NOT use a bare `cp -R`). Add a top-level `"files": ["dist"]` array so the compiled template ships in the npm tarball (package.json has `bin` but no `files`). Reject the alternatives RESEARCH names (embedding the template as a TS string; a bundler) — the copy step is the minimal convention-consistent fix. Create `test/dist-template.test.ts`: run `npm run build` (via execa or `child_process` with a generous timeout) once in the test, then assert `existsSync` is true for `dist/templates/agent-instructions.md.tmpl` AND that calling `seedInstructions` from the BUILT module layout (or asserting the file content matches `src/templates/agent-instructions.md.tmpl`) succeeds without ENOENT. This guard makes the Pitfall-9 regression (invisible under `tsx`) fail loudly in CI.</action>
  <verify>
    <automated>npm run build && node -e "process.exit(require('fs').existsSync('dist/templates/agent-instructions.md.tmpl')?0:1)" && npx vitest run test/dist-template.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` `build` script contains `cpSync` (or an equivalent node copy) copying `src/templates` → `dist/templates`; NOT a bare `tsc`
    - `package.json` has a top-level `"files": ["dist"]`
    - After `npm run build`, `dist/templates/agent-instructions.md.tmpl` exists
    - `test/dist-template.test.ts` asserts the built `.tmpl` exists / `seedInstructions` resolves against the dist layout; `npx vitest run test/dist-template.test.ts` passes
  </acceptance_criteria>
  <done>The built binary ships and finds its instruction template; a guard test prevents silent regression under tsx-only dev.</done>
</task>

<task type="auto">
  <name>Task 2: Pin claude --bare omission + strengthen ancestor-ignore directive + fix stale comments (carry-over gap 2)</name>
  <files>src/templates/agent-instructions.md.tmpl, src/workspace/scope.ts, src/protocol/instructions.ts, test/claude-adapter.test.ts, test/instructions.test.ts</files>
  <read_first>
    - src/adapters/claude.ts (buildArgv lines 20-24: `-p`, promptText, `--output-format json`, optional `--model` — NO `--bare`; doc-comment 12-19 explains WHY omitted)
    - src/templates/agent-instructions.md.tmpl (lines 15-17 ancestor-ignore directive; lines 19-32 OUTPUT CHANNEL section structure to mirror)
    - src/workspace/scope.ts (lines 46-51 comment claiming claude `--bare` is the neutralization flag — STALE, claude.ts omits it)
    - src/protocol/instructions.ts (lines 32-40 comment claiming claude MUST pass `--bare` — STALE)
    - test/claude-adapter.test.ts (existing flag-pinning test to extend with a `--bare`-absent assertion)
    - .planning/phases/05-hardening-resume-gating-majority-guards/05-RESEARCH.md (Q6b: accept status quo, strengthen seeded directive, fix stale comments; leakage measured zero on run 20260605-MlhRzU)
    - .planning/phases/05-hardening-resume-gating-majority-guards/05-PATTERNS.md (files #9 and #10)
  </read_first>
  <action>Do NOT add `--bare` to `src/adapters/claude.ts` (it breaks subscription/OAuth auth — the documented decision; leakage was measured zero live). In `test/claude-adapter.test.ts`, add a flag-pinning assertion that the claude argv does NOT include `--bare` (mirrors the existing argv-pin test) so a future re-introduction fails loudly. In `src/templates/agent-instructions.md.tmpl`, strengthen the existing ancestor-ignore directive (currently lines 15-17) toward the RESEARCH Q6b wording: an explicit instruction to read the in-folder vendor contract file (CLAUDE.md / AGENTS.md / GEMINI.md) as the SOLE format contract and to ignore any ancestor-directory or global instructions — phrase it so the literal token `ignore any ancestor` appears. Keep the section parallel in style to the OUTPUT CHANNEL section (lines 19-32). In `src/workspace/scope.ts` (the comment around lines 46-51) and `src/protocol/instructions.ts` (lines 32-40), correct the now-stale claims that the live claude adapter passes `--bare`: replace them with the accurate decision — claude `--bare` is intentionally OMITTED (auth-break), and ancestor neutralization is achieved by the seeded vendor file being the nearest contract PLUS the strengthened explicit ignore-ancestors directive in the template; codex `--ignore-user-config` / gemini folder-trust scoping remain the per-vendor mechanisms where applicable. Update `test/instructions.test.ts` only if its assertions reference the `--bare` claim; otherwise leave its seeding-spike assertions intact.</action>
  <verify>
    <automated>npx vitest run test/claude-adapter.test.ts test/instructions.test.ts && grep -c "ignore any ancestor" src/templates/agent-instructions.md.tmpl && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `src/adapters/claude.ts` buildArgv still omits `--bare` (unchanged); `test/claude-adapter.test.ts` asserts `--bare` is absent from the claude argv
    - `src/templates/agent-instructions.md.tmpl` contains the literal token `ignore any ancestor` in a strengthened sole-contract directive
    - `grep -n -- "--bare" src/workspace/scope.ts src/protocol/instructions.ts` shows the comments now state `--bare` is OMITTED, not passed (no remaining claim that the live adapter passes it)
    - `npx vitest run test/claude-adapter.test.ts test/instructions.test.ts` passes; `npx tsc --noEmit` clean
  </acceptance_criteria>
  <done>The `--bare` decision is pinned and documented accurately; the seeded contract does the ancestor neutralization explicitly; no stale comment misleads a maintainer into breaking auth.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| repo-root / ancestor instruction files → agent CLI context | ancestor CLAUDE.md (GSD directives) could dilute the seeded format contract if not neutralized |
| npm tarball → installed binary | a missing template asset silently breaks the packaged binary at first run |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | ancestor/global instruction files override the seeded format contract (independence/contract erosion) | mitigate | strengthened seeded "ignore any ancestor or global instructions" + explicit in-folder Read directive (Task 2); seeded file is the nearest contract; live run measured zero GSD-language leakage (Q6b) |
| T-05-02 | Denial of Service | packaged binary ENOENTs on the missing template at every run | mitigate | build copies the template into dist + `files:["dist"]`; guard test fails loudly on regression (Task 1) |
| T-05-03 | Spoofing | future drift re-adds `--bare`, breaking subscription auth (a self-inflicted outage) | mitigate | claude argv flag-pin asserts `--bare` absent (Task 2) |
</threat_model>

<verification>
- `npm run build` succeeds and `dist/templates/agent-instructions.md.tmpl` exists
- `npx vitest run` (full suite) green — no regressions
- `npx tsc --noEmit` clean; `npx biome check` clean
- `grep -- "--bare"` across scope.ts/instructions.ts shows only OMITTED-decision comments
</verification>

<success_criteria>
- The compiled binary seeds the format contract from dist (carry-over gap 1 closed)
- `--bare` stays omitted and is pinned; the ancestor-ignore directive is strengthened; stale comments corrected (carry-over gap 2 closed)
</success_criteria>

<output>
Create `.planning/phases/05-hardening-resume-gating-majority-guards/05-01-SUMMARY.md` when done
</output>
