---
phase: 05-hardening-resume-gating-majority-guards
plan: 03
type: execute
wave: 2
depends_on: ["05-02"]
files_modified:
  - src/protocol/converge.ts
  - test/converge.test.ts
autonomous: true
requirements: [RSLV-02, RSLV-03]
user_setup: []

must_haves:
  truths:
    - "After the convergence loop hits the cap OR a stable deadlock, a CLEAR majority of the surviving roster on one base resolves the fork as `agreed` with `resolver: \"majority\"` INSTEAD of escalating (D-59) — the evidence-grounded loop stays primary, majority is a tie-break only after cap/deadlock"
    - "Clear majority is `bestCount > rosterSize/2` via a NEW clearMajority helper — NOT mostSupportedBase, which returns a plurality on a 1-1 tie and would wrongly resolve D-60's escalate case (Pitfall 3)"
    - "A 3-vendor 2-1 split at cap/deadlock resolves via majority; a 3-vendor 1-1-1 split escalates; a 2-vendor 1-1 deadlock escalates (D-60 — 1 is not > half of 2)"
    - "ConvergenceResult gains an additive optional `resolver` field (convergence | majority | integrator | human); the unanimous-agreement return is tagged `resolver: \"convergence\"`, the majority return `resolver: \"majority\"` (D-61)"
    - "The running tally is computed ONLY at the exit boundary (post-cap/deadlock) and is NEVER injected into a round's prompt (D-59 anti-anchoring — preserve the independence core value)"
  artifacts:
    - path: "src/protocol/converge.ts"
      provides: "clearMajority helper + majority tie-break before both escalate guards + resolver field"
      contains: "clearMajority"
  key_links:
    - from: "src/protocol/converge.ts"
      to: "ConvergenceResult.resolver"
      via: "agreed/majority returns set resolver; decision-record (05-06) sources it"
      pattern: "resolver"
---

<objective>
The majority tie-break vertical slice (RSLV-02 + the no-clear-majority routing half of RSLV-03), a
~30-minute insert into converge.ts: add a `clearMajority(signals, rosterSize)` helper (`bestCount >
rosterSize/2`) and call it BEFORE `escalate(...)` in BOTH the cap guard (line 222) and the deadlock
guard (line 242). When a clear majority of the surviving roster proposes one base at the exit boundary,
return an `agreed` resolution tagged `resolver: "majority"` instead of escalating; with no clear
majority, escalate exactly as today (D-59/D-60). Tag the existing unanimous-agreement return
`resolver: "convergence"` (D-61). The tally is read only at the exit boundary — never injected into a
round (D-59 anti-anchoring).

Purpose: RSLV-02 needs discrete forks to use the agents' positions as a majority signal to break ties;
RSLV-03 needs no-clear-majority forks to still escalate. The tally primitives (tallyBases /
mostSupportedBase) already exist; this adds the threshold helper and the two insert points.
Output: clearMajority + resolver field + the two guard inserts, proven by 2-1 / 1-1-1 / 2-vendor-1-1
convergence fixtures.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-hardening-resume-gating-majority-guards/05-CONTEXT.md
@.planning/phases/05-hardening-resume-gating-majority-guards/05-RESEARCH.md
@.planning/phases/05-hardening-resume-gating-majority-guards/05-PATTERNS.md
@.planning/phases/04-first-end-to-end-run/04-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: clearMajority helper + resolver field + majority tie-break before both escalate guards (RSLV-02, D-59/D-61)</name>
  <files>src/protocol/converge.ts</files>
  <read_first>
    - src/protocol/converge.ts (lines 22-35 ConvergenceResult to extend; 118-136 tallyBases/mostSupportedBase; 210-219 the agreed return to tag; 221-249 the cap + deadlock escalate guards = the insert points; 260-297 integratorFor + escalate fallback)
    - src/schema/resolved-decisions.ts (the `Resolver` enum from 05-02 — reuse its union members for the ConvergenceResult.resolver type so the vocabulary is single-sourced)
    - .planning/phases/05-hardening-resume-gating-majority-guards/05-RESEARCH.md (Q4: clearMajority bestCount > size/2; insert BEFORE escalate in BOTH guards; mostSupportedBase stays the escalate fallback ONLY; Pitfall 3; Open Q4 — majority may fire at both guards, cost not a constraint)
    - .planning/phases/05-hardening-resume-gating-majority-guards/05-PATTERNS.md (file #3: exact excerpts + deviations — additive resolver field, anti-anchoring, 2-vendor 1-1 returns null)
  </read_first>
  <action>In `src/protocol/converge.ts`: (1) Add an optional `resolver` field to `ConvergenceResult` (type union `"convergence" | "majority" | "integrator" | "human"`, mirroring schema/resolved-decisions.ts `Resolver`) with a doc-comment in the existing optional-field style. (2) Add a pure `clearMajority(signals: RoundSignal[], rosterSize: number): string | null` helper that reuses `tallyBases`, finds the highest-count base, and returns that base ONLY when its count is strictly `> rosterSize/2`, else `null`. Do NOT reuse `mostSupportedBase` for this — it returns a plurality on a tie (Pitfall 3). (3) Tag the unanimous-agreement return (lines 210-219) with `resolver: "convergence"`. (4) Insert the tie-break BEFORE the cap-guard `escalate` (line 222) and BEFORE the deadlock-guard `escalate` (line 242): compute `const majorityBase = clearMajority(signals, roster.length)` and, when non-null, `return { base: majorityBase, integrator: integratorFor(majorityBase, signals), rounds: round, status: "agreed", concessions, resolver: "majority" }`. When null, fall through to the existing `escalate(...)` unchanged (the fallback path keeps using `mostSupportedBase`). `roster` is the surviving roster passed into `runConvergence` (its `.length` is the size for the threshold) — thread it (it is already a parameter). (5) Do NOT inject the tally into any round prompt — it is computed only here at the exit boundary (D-59). Leave the escalate() helper and mostSupportedBase untouched.</action>
  <verify>
    <automated>npx vitest run test/converge.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `converge.ts` contains `function clearMajority(` returning a base only when count `> rosterSize/2`, else null
    - `ConvergenceResult` has an optional `resolver` field; the unanimous return sets `resolver:"convergence"`, the majority return sets `resolver:"majority"`
    - `clearMajority` is called before BOTH the cap-guard and deadlock-guard `escalate(...)` calls; `mostSupportedBase` is still used ONLY inside `escalate`
    - grep shows the tally is not added to any prompt string (no new call into runPhase prompt path with tally data — anti-anchoring preserved)
    - `npx tsc --noEmit` clean
  </acceptance_criteria>
  <done>A clear majority breaks a post-cap/deadlock tie via resolver:"majority"; no-clear-majority still escalates; the tally never anchors a round.</done>
</task>

<task type="auto">
  <name>Task 2: Majority convergence tests — 2-1 resolves, 1-1-1 escalates, 2-vendor 1-1 escalates (D-60, Pitfall 3)</name>
  <files>test/converge.test.ts</files>
  <read_first>
    - test/converge.test.ts (lines 1-70: the convergence unit-test harness — mkdtemp runDir, createRun, writeEvalFixture(dir, author, vendor, proposedBase, disagreements), runConvergence(roster, input))
    - src/protocol/converge.ts (the modified loop from Task 1 — clearMajority, resolver, the cap/deadlock guards; cap default 10, UNRESOLVABLE_STABLE_ROUNDS=2)
    - test/fixtures/structured-shared.mjs (the per-author MAR_EMIT_BASES steering added in 05-02 — used if a test drives via fixtures; the converge unit harness writes eval artifacts directly via writeEvalFixture)
    - .planning/phases/05-hardening-resume-gating-majority-guards/05-RESEARCH.md (RSLV-02 validation architecture: divergent proposedBase drives to cap/deadlock; 2-1 → resolver:"majority"; 1-1-1 → escalate; 2-vendor 1-1 → escalate with openDecision)
  </read_first>
  <action>Extend `test/converge.test.ts` with majority cases using the existing `writeEvalFixture` harness to seed DIVERGENT per-author `proposedBase` values so the loop reaches a guard without unanimous agreement (set a low `convergenceCap` in the test config, or drive enough rounds to hit the stable-deadlock guard — mirror the existing cap/deadlock tests' setup): (1) THREE-vendor 2-1 split that persists to the cap/deadlock boundary → assert the result is `status:"agreed"`, `resolver:"majority"`, and `base` is the 2-supported base, with NO `openDecision`. (2) THREE-vendor 1-1-1 split at the boundary → assert `status:"escalated"` with an `openDecision` and NO `resolver:"majority"` (clearMajority returned null). (3) TWO-vendor 1-1 split at the boundary → assert `status:"escalated"` with an `openDecision` (D-60: 1 is not > half of 2). (4) A unanimous-agreement case → assert `resolver:"convergence"` (the existing happy-path test can be extended with this assertion). Keep the existing cap/deadlock tests passing.</action>
  <verify>
    <automated>npx vitest run test/converge.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - A 3-vendor 2-1 boundary case asserts `status:"agreed"` + `resolver:"majority"` + the 2-supported base, no openDecision
    - A 3-vendor 1-1-1 boundary case asserts `status:"escalated"` + openDecision, no `resolver:"majority"`
    - A 2-vendor 1-1 boundary case asserts `status:"escalated"` + openDecision (D-60)
    - A unanimous case asserts `resolver:"convergence"`
    - `npx vitest run test/converge.test.ts` passes
  </acceptance_criteria>
  <done>The majority tie-break is proven on hermetic divergent-base fixtures across the three split shapes plus the unanimous tag.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| per-round evaluation artifacts (proposedBase) → tally | attacker-influenceable model output; tally reads only validated frontmatter fields, never prose self-report |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-07 | Spoofing | a 1-1 tie wrongly resolved as a majority (mostSupportedBase plurality) bypassing escalation | mitigate | clearMajority uses `> rosterSize/2`, returns null on a tie; mostSupportedBase reserved for the escalate fallback (Pitfall 3); 2-vendor-1-1 + 1-1-1 escalate tests |
| T-05-08 | Tampering | injecting the running tally into a round prompt anchors agents toward the leading base (erodes independence) | mitigate | tally computed ONLY at the exit boundary, never added to a round prompt (D-59); grep assertion |
</threat_model>

<verification>
- `npx vitest run test/converge.test.ts` green (majority + existing cap/deadlock cases)
- `npx vitest run` (full suite) green — no regressions
- `npx tsc --noEmit` clean; `npx biome check` clean
</verification>

<success_criteria>
- A clear majority breaks a post-cap/deadlock tie (RSLV-02); no-clear-majority escalates (RSLV-03 routing)
- resolver field distinguishes convergence vs majority (D-61); the tally never anchors a round (D-59)
</success_criteria>

<output>
Create `.planning/phases/05-hardening-resume-gating-majority-guards/05-03-SUMMARY.md` when done
</output>
