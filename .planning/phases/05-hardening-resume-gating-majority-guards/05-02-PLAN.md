---
phase: 05-hardening-resume-gating-majority-guards
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/protocol/frontmatter.ts
  - src/schema/manifest.ts
  - src/schema/resolved-decisions.ts
  - src/protocol/converge.ts
  - src/protocol/decision-record.ts
  - test/fixtures/structured-shared.mjs
  - test/frontmatter.test.ts
  - test/manifest.test.ts
autonomous: true
requirements: [RSLV-02, RCRD-02, PROT-06]
user_setup: []

must_haves:
  truths:
    - "ONE shared tolerant frontmatter reader exists (src/protocol/frontmatter.ts) and the two strict double-parse copies in converge.ts and decision-record.ts now call it — fixing the strict-vs-tolerant divergence (Pitfall 4: a preamble-prefixed artifact passed the live gate but the strict reader silently dropped it)"
    - "The manifest status enum gains the non-terminal `paused-awaiting-approval`; prior manifests parse unchanged (additive)"
    - "RESUMABLE_STATUSES (running, failed, timeout, paused-awaiting-approval) and TERMINAL_DONE (completed, escalated) are defined in ONE place so resumability is the single explicit source (Q7) — terminal-vs-resumable is enforced nowhere else today"
    - "A new src/schema/resolved-decisions.ts zod module carries the rolling-ledger shape with a `resolver` enum (convergence | majority | integrator | human, D-61), additive so an empty ledger parses"
    - "The fixture can steer a DIFFERENT proposedBase per author (the one real fixture change RSLV-02 needs) without breaking the existing single-base MAR_EMIT_BASE behavior"
  artifacts:
    - path: "src/protocol/frontmatter.ts"
      provides: "shared tolerant agent-frontmatter reader (wrapper-strip + first-`---` fallback)"
      contains: "readAgentFrontmatter"
    - path: "src/schema/resolved-decisions.ts"
      provides: "rolling-ledger zod schema + resolver enum (D-61)"
      contains: "resolver"
    - path: "src/schema/manifest.ts"
      provides: "paused-awaiting-approval status + RESUMABLE/TERMINAL sets"
      contains: "paused-awaiting-approval"
  key_links:
    - from: "src/protocol/converge.ts"
      to: "src/protocol/frontmatter.ts"
      via: "readEvaluationSignal calls the shared tolerant reader instead of its own strict double-parse"
      pattern: "readAgentFrontmatter"
    - from: "src/protocol/decision-record.ts"
      to: "src/protocol/frontmatter.ts"
      via: "readAgentFrontmatter delegates to the shared tolerant reader"
      pattern: "readAgentFrontmatter"
---

<objective>
The Wave-1 shared foundation every later slice needs, kept small: (1) extract the tolerant
`parseFront` (engine.ts:198-206) into ONE shared `src/protocol/frontmatter.ts` reader and replace the
two near-duplicate STRICT double-parses in converge.ts (readEvaluationSignal) and decision-record.ts
(readAgentFrontmatter) — eliminating the latent strict-vs-tolerant divergence (Pitfall 4) so resume
re-validation (05-04) and the decision record both read live-model artifacts identically; (2) add the
non-terminal `paused-awaiting-approval` status + a single RESUMABLE/TERMINAL source (Q7) consumed by
resume (05-04) and pause-and-exit (05-05); (3) add the `src/schema/resolved-decisions.ts` ledger schema
+ `resolver` enum (D-61) consumed by majority (05-03) and the re-litigation guard (05-06); (4) extend
the fixture for per-author base steering (the one real fixture change RSLV-02 needs).

Purpose: these are the shared primitives multiple downstream slices reference; folding them into one
disjoint Wave-1 plan keeps later waves from colliding on schema/reader edits. No behavior change to the
running protocol beyond the reader unification (which is a pure bugfix toward the tolerant variant).
Output: the shared reader (callers re-pointed), the status enum + sets, the ledger schema, and the
per-author fixture steering — all green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-hardening-resume-gating-majority-guards/05-CONTEXT.md
@.planning/phases/05-hardening-resume-gating-majority-guards/05-RESEARCH.md
@.planning/phases/05-hardening-resume-gating-majority-guards/05-PATTERNS.md
@.planning/phases/04-first-end-to-end-run/04-05-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extract the shared tolerant frontmatter reader; re-point the two strict callers (Pitfall 4)</name>
  <files>src/protocol/frontmatter.ts, src/protocol/converge.ts, src/protocol/decision-record.ts, test/frontmatter.test.ts</files>
  <read_first>
    - src/protocol/engine.ts (lines 191-206: the TOLERANT `parseFront` — direct `matter().data`, else fall back to the FIRST `^---$` delimiter line; this is the variant to lift)
    - src/protocol/converge.ts (lines 82-102: readEvaluationSignal — the STRICT double-parse `matter(raw)` → `matter(outer.content.trimStart())`; note the `.trimStart()` and the EvaluationFrontmatter.safeParse it wraps)
    - src/protocol/decision-record.ts (lines 100-110: readAgentFrontmatter — the STRICT double-parse to replace)
    - .planning/phases/05-hardening-resume-gating-majority-guards/05-RESEARCH.md (Q3: one shared tolerant reader; Pitfall 4 detection — a preamble-prefixed artifact must re-validate AND contribute to the record)
    - .planning/phases/05-hardening-resume-gating-majority-guards/05-PATTERNS.md (file #7: the reader must strip the engine-metadata wrapper FIRST then tolerantly parse the inner body; do not skip `.trimStart()`)
  </read_first>
  <action>Create `src/protocol/frontmatter.ts` exporting an async `readAgentFrontmatter(path: string): Promise<unknown | null>` plus a pure `parseAgentFrontmatter(raw: string): unknown` (so resume re-validation in 05-04 can validate already-read text without a second read). The reader must: read the file (return null on missing/unreadable, preserving the existing non-signal semantics); strip the engine-metadata wrapper with `matter(raw)` then `.trimStart()` the inner body (gray-matter only recognizes frontmatter at position 0); then apply the TOLERANT fallback from engine.ts:198-206 — if the direct `matter(inner).data` has keys use it, else match the first `^---\s*$` delimiter line and parse from there. Keep gray-matter READ-only (no `matter.stringify`); default js-yaml SAFE load (T-04-07). Re-point `converge.ts` readEvaluationSignal and `decision-record.ts` readAgentFrontmatter to call this shared function (they keep their own zod `safeParse` of the returned data — leniency is about WHERE frontmatter is found, never its shape, D-38). Do NOT yet change engine.ts's inline `parseFront` (05-04 owns the engine resume path and will consolidate it; leaving engine.ts untouched here keeps this plan disjoint from the engine-touching waves). Create `test/frontmatter.test.ts` asserting: a wrapper+clean-agent-frontmatter artifact reads the agent fields; a wrapper+PREAMBLE-PROSE-then-`---` artifact still reads the agent fields (the tolerant path — this is the Pitfall-4 regression guard, which the OLD strict double-parse would have dropped); a missing file returns null.</action>
  <verify>
    <automated>npx vitest run test/frontmatter.test.ts test/converge.test.ts test/decision-record.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `src/protocol/frontmatter.ts` exports `readAgentFrontmatter` and `parseAgentFrontmatter`; uses gray-matter READ-only (grep: no `stringify`)
    - `converge.ts` readEvaluationSignal and `decision-record.ts` readAgentFrontmatter both call the shared reader (grep both files import from `./frontmatter.js`)
    - `test/frontmatter.test.ts` proves a preamble-prefixed agent artifact reads its fields (tolerant path) where a strict double-parse would return empty data
    - `npx vitest run test/frontmatter.test.ts test/converge.test.ts test/decision-record.test.ts` passes; `npx tsc --noEmit` clean
  </acceptance_criteria>
  <done>One tolerant reader serves converge + decision-record (and is ready for resume); the strict-vs-tolerant divergence is gone and guarded by a preamble-artifact test.</done>
</task>

<task type="auto">
  <name>Task 2: paused-awaiting-approval status + RESUMABLE/TERMINAL sets (Q7)</name>
  <files>src/schema/manifest.ts, test/manifest.test.ts</files>
  <read_first>
    - src/schema/manifest.ts (lines 36-54: the `status` z.enum + the `escalated` additive-status doc-comment precedent; ManifestStatus type export)
    - src/workspace/manifest.ts (lines 119-135: setStatus is generic over ManifestStatus — no terminal-only assumption, so writing the new status needs no change)
    - .planning/phases/05-hardening-resume-gating-majority-guards/05-RESEARCH.md (Q7: add the status; define RESUMABLE_STATUSES {running, failed, timeout, paused-awaiting-approval} + TERMINAL_DONE {completed, escalated} once; terminal-vs-resumable enforced nowhere today; Pitfall 6)
    - .planning/phases/05-hardening-resume-gating-majority-guards/05-PATTERNS.md (file #4)
  </read_first>
  <action>In `src/schema/manifest.ts` add `"paused-awaiting-approval"` to the `status` z.enum (mirror the `escalated` additive-status doc-comment so prior manifests parse unchanged — it is NON-terminal). Export two readonly sets/arrays in this same module (the single source, Q7): `RESUMABLE_STATUSES` containing `running`, `failed`, `timeout`, `paused-awaiting-approval` (D-57: failed/timeout ARE resumable), and `TERMINAL_DONE` containing `completed`, `escalated`. Type them against `ManifestStatus` so adding/removing an enum member surfaces a compile error if the sets drift. Do NOT add any status-reading branch elsewhere (no code reads `manifest.status` to gate behavior today — the resume command in 05-04 is the only new reader). Extend `test/manifest.test.ts` to assert: `setStatus(runDir, "paused-awaiting-approval")` round-trips through readManifest; `RESUMABLE_STATUSES` includes the four resumable statuses and EXCLUDES `completed`/`escalated`; `TERMINAL_DONE` is exactly `completed`/`escalated`.</action>
  <verify>
    <automated>npx vitest run test/manifest.test.ts && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `src/schema/manifest.ts` `status` enum includes `"paused-awaiting-approval"`; existing manifests still `Manifest.parse` (additive)
    - `RESUMABLE_STATUSES` and `TERMINAL_DONE` are exported from `src/schema/manifest.ts`; RESUMABLE has the 4 statuses, TERMINAL_DONE has exactly `completed`+`escalated`
    - `setStatus(runDir, "paused-awaiting-approval")` round-trips — asserted in test/manifest.test.ts
    - `npx vitest run test/manifest.test.ts` passes; `npx tsc --noEmit` clean
  </acceptance_criteria>
  <done>The non-terminal paused status exists and resumability has one explicit, typed, testable source.</done>
</task>

<task type="auto">
  <name>Task 3: resolved-decisions ledger schema + resolver enum (D-61) + per-author fixture base steering</name>
  <files>src/schema/resolved-decisions.ts, test/fixtures/structured-shared.mjs</files>
  <read_first>
    - src/schema/decision-record.ts (lines 14-21 ResolvedDecision shape to mirror; .min(1) for required strings, .default([]) for additive arrays)
    - src/schema/evaluation.ts (z.literal discriminator precedent for a `phase`-like field)
    - .planning/phases/05-hardening-resume-gating-majority-guards/05-RESEARCH.md (Q5: new zod schema mirroring ResolvedDecision + resolver field; D-61 enum convergence|majority|integrator|human; an empty ledger must parse; Open Q3 per-author base env shape)
    - .planning/phases/05-hardening-resume-gating-majority-guards/05-PATTERNS.md (file #5 schema-module shape; file #11 fixture per-author steering — structured-shared.mjs:33-36 proposedBase, argv helpers :14-26)
    - test/fixtures/structured-shared.mjs (lines 28-36: proposedBase(author) currently returns process.env.MAR_EMIT_BASE || author — extend to per-author)
  </read_first>
  <action>Create `src/schema/resolved-decisions.ts` following the established schema-module shape (`import { z } from "zod"` → small named building blocks → exported object schema → `export type X = z.infer<typeof X>`). Define a `Resolver` enum `z.enum(["convergence", "majority", "integrator", "human"])` (D-61) and a `ResolvedDecisionEntry` object mirroring schema/decision-record.ts ResolvedDecision (`id`, `summary`, `rationale` all `.min(1)`, `lineage: z.array(...).default([])`) PLUS a `resolver: Resolver` field. Define a `ResolvedDecisionsLedger` frontmatter object: `runId: z.string().min(1)` and `decisions: z.array(ResolvedDecisionEntry).default([])` (additive — a run with zero settled forks must produce a parseable empty ledger). Add rich doc-comments citing D-61/D-63 and the additive-enum style from schema/manifest.ts. In `test/fixtures/structured-shared.mjs` extend `proposedBase(author)` for PER-AUTHOR steering (the one real fixture change RSLV-02 needs, Open Q3): honor a JSON env `MAR_EMIT_BASES` (e.g. `{"claude":"claude","codex":"codex"}`) returning that author's mapped base when present, FALLING BACK to the existing single `MAR_EMIT_BASE`, then to the author's own name — so existing tests using `MAR_EMIT_BASE` are unaffected. Parse the JSON defensively (a malformed `MAR_EMIT_BASES` falls back rather than throwing).</action>
  <verify>
    <automated>node -e "import('./src/schema/resolved-decisions.ts').catch(()=>{})" ; MAR_EMIT_BASES='{"claude":"codex"}' node -e "import('./test/fixtures/structured-shared.mjs').then(m=>process.exit(m.proposedBase('claude')==='codex'?0:1))" && npx tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - `src/schema/resolved-decisions.ts` exports `Resolver` (enum convergence|majority|integrator|human), `ResolvedDecisionEntry` (id/summary/rationale/lineage/resolver), and `ResolvedDecisionsLedger` ({runId, decisions:[]}) with `.infer` types
    - An empty `{ runId, decisions: [] }` parses (additive ledger)
    - `MAR_EMIT_BASES='{"claude":"codex"}'` makes `proposedBase("claude")` return `"codex"`; with neither env set `proposedBase("claude")` returns `"claude"`; a bare `MAR_EMIT_BASE=x` still forces `x` (backward compatible)
    - `npx tsc --noEmit` clean
  </acceptance_criteria>
  <done>The ledger schema + resolver enum exist for the majority and guard slices, and the fixture can steer a divergent per-author base for the RSLV-02 majority tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| agent CLI stdout → orchestrator (frontmatter) | attacker-influenceable; the SHARED reader must keep gray-matter READ-only + js-yaml SAFE load |
| persisted manifest/ledger schema | additive enum/field changes must not silently re-interpret prior on-disk state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | Tampering | a preamble-prefixed valid artifact silently dropped by the strict reader (tally/record/resume miss) | mitigate | one shared tolerant reader; preamble-artifact regression test (Task 1) |
| T-05-05 | Elevation of Privilege | YAML deserialization via crafted ledger/agent frontmatter | mitigate | gray-matter default js-yaml SAFE load; reader stays READ-only; ledger WRITES use the hand-rolled serializer in 05-06, never matter.stringify (T-04-07) |
| T-05-06 | Tampering | a future reader treats `paused-awaiting-approval` as terminal/done | mitigate | single RESUMABLE_STATUSES/TERMINAL_DONE source, typed against ManifestStatus (Task 2, Pitfall 6) |
</threat_model>

<verification>
- `npx vitest run test/frontmatter.test.ts test/converge.test.ts test/decision-record.test.ts test/manifest.test.ts` green
- `npx vitest run` (full suite) green — no regressions
- `npx tsc --noEmit` clean; `npx biome check` clean
</verification>

<success_criteria>
- One tolerant frontmatter reader replaces the two strict copies (Pitfall 4 fixed)
- `paused-awaiting-approval` + RESUMABLE/TERMINAL sets exist as the single resumability source
- The resolved-decisions ledger schema + resolver enum exist; the fixture steers per-author bases
</success_criteria>

<output>
Create `.planning/phases/05-hardening-resume-gating-majority-guards/05-02-SUMMARY.md` when done
</output>
