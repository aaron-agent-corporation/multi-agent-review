---
phase: 03-protocol-engine-independence-enforcement
plan: 03
type: execute
wave: 3
depends_on: ["03-02"]
files_modified:
  - test/planted-error.test.ts
  - test/fixtures/fake-claude.mjs
  - test/fixtures/fake-codex.mjs
autonomous: false
requirements: [PROT-04, PROT-01]
must_haves:
  truths:
    - "An A/B hermetic test proves independent drafts surface a planted error that a shared-context control run masks (success criterion #4)"
    - "The control arm (all agents see one consensus draft carrying the planted error) does NOT surface the error; the treatment arm (independent scoped drafts) DOES surface a discrepancy at cross-review"
    - "The A/B test uses deterministic fake-CLI fixtures only — zero credits, hermetic, CI-gateable"
    - "A single live human-verified `mar run` on a small real document advances through all 6 phases (the once-per-phase live check, mirroring 01/02)"
  artifacts:
    - path: "test/planted-error.test.ts"
      provides: "A/B independence proof (success criterion #4)"
      contains: "control"
  key_links:
    - from: "test/planted-error.test.ts"
      to: "runProtocol / mar run"
      via: "drives both arms through the engine"
      pattern: "run"
---

<objective>
Prove the project's core thesis structurally and verify it live. Build the planted-error A/B test (success criterion #4): a control arm where all agents share one consensus draft carrying a planted error masks the error; a treatment arm with independent scoped drafts surfaces it at cross-review. Then a single live human-verified `mar run` on a small real document confirms the engine works end-to-end against real CLIs (the once-per-phase live check that CI fixtures cannot give).

Purpose: Without the A/B control arm, "independent run caught the error" proves nothing about independence (Pitfall 2). This test is the empirical justification for the whole independence-enforcement design. The live checkpoint mirrors the Phase 1/2 pattern: automated tests stay hermetic/zero-credit; one human-verified live run closes the phase.
Output: `test/planted-error.test.ts` (hermetic A/B, CI-gateable) + any fixture mode it needs + one live human-verified run.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/03-protocol-engine-independence-enforcement/03-RESEARCH.md
@.planning/phases/03-protocol-engine-independence-enforcement/03-02-SUMMARY.md

<interfaces>
<!-- Use the engine + fixtures built in Plans 01-02. -->

`mar run <input>` and `runProtocol(runDir, config, inputPath)` exist (Plan 02). The fake fixtures support an `--emit <kind>` mode (Plan 01) and the standard happy/failure modes.

Fixture extension precedent (test/fixtures/fake-codex.mjs already has a STATEFUL mode `--rate-limit-once` using a marker file under MAR_FIXTURE_STATE_DIR — reuse this technique if the A/B arms need a fixture that emits a planted-error value vs. a correcting value depending on draft isolation).

e2e harness pattern (test/e2e-invoke.test.ts, test/protocol-run.e2e.test.ts): mkdtempSync workdir; write mar.config.json injecting fake bins (>=2 distinct vendors: claude + codex); drive `npx tsx src/cli.ts run <input>` via execa reject:false; assert on-disk manifest + artifacts; vi.setConfig({ testTimeout: 60_000 }).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build the planted-error A/B independence proof (hermetic, success criterion #4)</name>
  <files>test/planted-error.test.ts, test/fixtures/fake-claude.mjs, test/fixtures/fake-codex.mjs</files>
  <read_first>
    - test/protocol-run.e2e.test.ts (e2e harness to mirror)
    - test/fixtures/fake-claude.mjs and test/fixtures/fake-codex.mjs (existing modes incl. the codex stateful marker-file pattern)
    - .planning/phases/03-protocol-engine-independence-enforcement/03-RESEARCH.md (Pitfall 2 A/B design lines 286-291; Open Question 3 lines 384-387)
    - src/protocol/engine.ts and src/workspace/scope.ts (how drafts are isolated/promoted — the mechanism the test exercises)
  </read_first>
  <action>
    Design a deterministic A/B test of independence. Add a fixture mode (e.g. `--planted-error` and a paired `--independent-correct`, or reuse the MAR_FIXTURE_STATE_DIR marker technique) so a fake agent emits a draft containing a known planted error value (e.g. a wrong constant) when it can "see a peer consensus draft", and emits a different/correcting value when drafting in isolation. CONTROL ARM: configure the run (or a shaped variant) so all agents share one consensus draft carrying the planted error; assert that no cross-review artifact flags the error — the error survives. TREATMENT ARM: a normal `mar run` where the draft phase is scoped (each agent isolated); assert that at least one agent's independent draft differs, producing a discrepancy that a review-phase artifact surfaces (e.g. the run's review artifacts contain the disagreement marker the fixtures emit on value mismatch). The test must be hermetic (fake fixtures only, zero credits) and assert BOTH arms (control masks, treatment surfaces) so it actually proves the claim. Keep all pre-existing fixture modes working. If reproducing a true "shared-context" control inside the current engine is not directly expressible (the engine always scopes the draft phase), construct the control arm by pre-seeding shared/ with a single consensus draft and running only the review phase, OR by a documented test-only shared-workdir variant — pick the approach that most faithfully shows masking and document the choice in a top-of-file comment.
  </action>
  <verify>
    <automated>npx vitest run test/planted-error.test.ts && npx vitest run && npx tsc --noEmit && npx biome check test/planted-error.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run test/planted-error.test.ts` passes with TWO asserted arms: a control case asserting the planted error is NOT surfaced, and a treatment case asserting it IS surfaced.
    - `grep -ci "control" test/planted-error.test.ts` >= 1 and `grep -ci "treatment\|independent" test/planted-error.test.ts` >= 1 (both arms present).
    - The test calls no real `claude`/`codex` binary — only `node <fixture>` bins (source assertion: config bins reference the fixtures).
    - `npx vitest run` — full suite green.
    - Pre-existing fixture modes still pass (no regression: e2e-invoke + adapter tests green).
    - tsc + biome clean.
  </acceptance_criteria>
  <done>A hermetic A/B test demonstrates independent drafts catch a planted error a shared-context run masks — success criterion #4 met with a real control arm.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Live human-verified `mar run` on a small real document</name>
  <what-built>The full Phase-3 protocol engine: `mar run <input>` drives a 2+-vendor roster through all 6 phases (draft -> review -> response -> evaluation -> integration -> validation) with artifacts-on-disk gating and structural draft independence. All automated tests are hermetic (fake fixtures, zero credits), so this is the once-per-phase live check that confirms the engine works against real CLIs — mirroring the Phase 1/2 live-verify pattern.</what-built>
  <how-to-verify>
    1. Ensure a real roster with >=2 distinct healthy vendors (claude + codex; gemini may be skipped per applySkipFailed if its headless auth is still broken). Run `mar preflight` to confirm.
    2. Create a tiny input document (a few sentences, e.g. a short paragraph to review).
    3. Run: `npx tsx src/cli.ts run <path-to-input.md>` (or the built `mar run <input>`).
    4. Confirm on disk under `runs/<id>/`:
       - manifest.json status ends "completed"
       - one artifact per agent for each of the 6 kinds: draft, review, response, evaluation, integration, validation
       - during/after the run, `work/<agent>/` for the draft phase contained only input.md (no peer draft); `shared/` contains the promoted drafts
       - invocations.ndjson logged each turn (command, exit, duration) with NO prompt body
    5. Confirm the console streamed a readable progress line per phase boundary / agent turn.
    6. Sanity-check that a single-vendor roster is refused: `mar run <input>` with a 1-vendor config exits non-zero with the >=2-vendor message.
  </how-to-verify>
  <resume-signal>Type "approved" once the live run completed all 6 phases with the expected artifacts and the single-vendor refusal held, or describe what went wrong.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| live CLI subprocesses -> filesystem | a real run writes artifacts under runs/<id>/; scoping must hold against real CLI behavior |
| test fixtures -> credits | tests must never invoke real billable CLIs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-11 | Information Disclosure | live draft phase | mitigate | live checkpoint explicitly verifies work/<agent>/ lacked peer drafts and promotion happened only at the boundary (PROT-04 confirmed against real CLIs, esp. codex under non-repo cwd — Pitfall 4) |
| T-03-12 | DoS / cost | accidental live invocation in CI | mitigate | A/B and all automated tests use `node <fixture>` bins only (zero credits); acceptance criteria assert no real-binary reference; the live run is a one-time human checkpoint, not a CI test |
| T-03-13 | Tampering | codex rollout/session files under scoped cwd | mitigate | live checkpoint inspects work/<agent>/ for stray session files; --ephemeral pinned (verify holds under the new cwd) |
| T-03-SC | Tampering | npm installs | accept | This plan adds NO new packages — no install action |
</threat_model>

<verification>
- `npx vitest run test/planted-error.test.ts` green with both control + treatment arms.
- `npx vitest run` full suite green; tsc + biome clean.
- Human-verified live `mar run` completed all 6 phases with correct artifacts, draft isolation held, and the single-vendor refusal fired.
</verification>

<success_criteria>
- Success criterion #4 met: a hermetic A/B test proves independent drafts catch an error a shared-context run masks.
- A live run confirms PROT-01/03/04 against real CLIs (once-per-phase live check).
- Zero credits burned by automated tests.
</success_criteria>

<output>
Create `.planning/phases/03-protocol-engine-independence-enforcement/03-03-SUMMARY.md` when done.
</output>
