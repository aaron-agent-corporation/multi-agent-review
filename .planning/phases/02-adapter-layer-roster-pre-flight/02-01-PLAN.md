---
phase: 02-adapter-layer-roster-pre-flight
plan: 01
type: tdd
wave: 1
depends_on: []
files_modified:
  - src/adapters/common.ts
  - src/adapters/claude.ts
  - src/adapters/codex.ts
  - src/adapters/gemini.ts
  - src/adapters/registry.ts
  - src/schema/turn.ts
  - test/fixtures/fake-codex.mjs
  - test/fixtures/fake-gemini.mjs
  - test/codex-adapter.test.ts
  - test/gemini-adapter.test.ts
  - test/registry.test.ts
autonomous: true
requirements: [ORCH-03]
must_haves:
  truths:
    - "codex adapter drives `codex exec --json`, parses NDJSON, returns a normalized TurnResult with ok keyed off turn.completed + exit 0"
    - "gemini adapter drives `gemini -p --output-format json`, parses JSON from stdout-OR-stderr, returns normalized TurnResult"
    - "a vendor->adapter registry returns the correct adapter for claude|codex|gemini; adding a vendor is one map entry"
    - "all three adapters satisfy the unchanged AgentAdapter interface — no vendor field names leak into TurnResult"
    - "each factory captures an optional model in closure and buildArgv appends the vendor's model flag (claude/gemini `--model <model>`, codex `-m <model>`)"
  artifacts:
    - path: "src/adapters/codex.ts"
      provides: "makeCodexAdapter(bin?, model?) — NDJSON terminal-event adapter; model captured in closure -> `-m <model>` argv"
      exports: ["makeCodexAdapter"]
    - path: "src/adapters/gemini.ts"
      provides: "makeGeminiAdapter(bin?, model?) — stdout-or-stderr JSON adapter; model captured in closure -> `-m <model>` argv"
      exports: ["makeGeminiAdapter"]
    - path: "src/adapters/registry.ts"
      provides: "makeAdapter(vendor, bin?, model?) factory — the ORCH-03 seam; authoritative signature"
      exports: ["makeAdapter"]
    - path: "src/schema/turn.ts"
      provides: "CodexEvent + GeminiJson zod schemas alongside ClaudeJson"
      contains: "CodexEvent"
    - path: "test/fixtures/fake-codex.mjs"
      provides: "NDJSON fixture: happy/--fail-auth/--rate-limit/--bad-json/--hang"
    - path: "test/fixtures/fake-gemini.mjs"
      provides: "JSON fixture: happy/--fail-auth(stderr,exit41)/--untrusted(exit55)/--rate-limit/--bad-json/--hang"
  key_links:
    - from: "src/adapters/registry.ts"
      to: "src/adapters/{claude,codex,gemini}.ts"
      via: "FACTORIES map keyed on vendor literal"
      pattern: "makeCodexAdapter|makeGeminiAdapter|makeClaudeAdapter"
    - from: "src/adapters/codex.ts"
      to: "src/schema/turn.ts"
      via: "CodexEvent.safeParse per NDJSON line"
      pattern: "CodexEvent"
---

<objective>
Build the two new vendor adapters (codex, gemini) and the vendor->adapter registry that make ORCH-03 true: all three vendors invoke through the same `AgentAdapter` contract with no protocol-layer branching on vendor. Extract the vendor-agnostic adapter utilities into a shared module so codex/gemini import rather than copy-paste claude's helpers.

Purpose: This is the adapter half of the phase — the uniform contract every later module (retry, preflight, roster CLI) composes. Without it nothing else in Phase 2 has three vendors to drive.

Output: `src/adapters/common.ts`, `src/adapters/codex.ts`, `src/adapters/gemini.ts`, `src/adapters/registry.ts`, refactored `src/adapters/claude.ts` (helpers move to common.ts — behavior byte-identical), extended `src/schema/turn.ts`, two fake-CLI fixtures, three test files. Gemini is FIXTURE-BUILT (D-32) — real gemini auth is broken on this machine and that is expected; CI must NOT gate on a live gemini success.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-adapter-layer-roster-pre-flight/02-CONTEXT.md
@.planning/phases/02-adapter-layer-roster-pre-flight/02-PATTERNS.md
@.planning/phases/02-adapter-layer-roster-pre-flight/02-RESEARCH.md

<interfaces>
<!-- The UNCHANGED Phase-1 contract codex/gemini implement. Use directly — no exploration. -->

From src/adapters/adapter.ts:
```typescript
export interface TurnRequest {
  agent: string; promptText: string; runDir: string; seq: number; timeoutMs: number;
}
export interface AgentAdapter { readonly name: string; invoke(req: TurnRequest): Promise<TurnResult>; }
```

From src/schema/turn.ts (TurnResult — normalize INTO this, do not change it):
```typescript
TurnResult = { ok, agent, text, exitCode, durationMs, timedOut, redactedCommand: string[],
  costUsd?, sessionId?, structuredOutput?, error? }
```

From src/adapters/claude.ts (the reference adapter — copy its skeleton; these helpers are exported/extractable):
- splitBin(bin) -> { cmd, preArgs }  (already exported)
- safeJsonParse(text) -> unknown  (currently private — move to common.ts)
- redactArgv(argv, promptText) + PROMPT_PLACEHOLDER="<prompt>"  (currently private — move to common.ts)
- execa opts: { timeout: req.timeoutMs, killSignal:"SIGTERM", forceKillAfterDelay:5000, reject:false, cleanup:true }
- timeout guard: if (result.timedOut || result.isForcefullyTerminated) -> { ok:false, timedOut:true, error:"timeout" }

Model-param contract (PINNED — authoritative for this plan and Plan 03/05):
- Registry: `makeAdapter(vendor: keyof typeof FACTORIES, bin?: string, model?: string): AgentAdapter` is the authoritative signature.
- Each factory: `make*Adapter(bin?: string, model?: string): AgentAdapter` captures `model` in closure.
- buildArgv appends the vendor's model flag when model is set: claude `--model <model>`, gemini `--model <model>` (alias `-m`), codex `-m <model>`.
- The roster (Plan 03) supplies `entry.model`; the CLI (Plan 05) threads it through `makeAdapter(vendor, bin, model)`. Adapters take model via the factory closure NOW — no deferral.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extract shared adapter common + add CodexEvent/GeminiJson schemas + write fixtures (RED)</name>
  <read_first>
    - src/adapters/claude.ts (source of splitBin/safeJsonParse/redactArgv/PROMPT_PLACEHOLDER — extract verbatim)
    - src/schema/turn.ts (ClaudeJson style to mirror; TurnResult stays UNCHANGED)
    - test/fixtures/fake-claude.mjs (fixture structure to replicate exactly)
    - test/claude-adapter.test.ts (test structure incl. flag-pinning vi.doMock pattern)
    - .planning/phases/02-adapter-layer-roster-pre-flight/02-RESEARCH.md (LIVE-VERIFIED codex NDJSON + gemini failure shapes; Code Examples section for exact schemas)
  </read_first>
  <behavior>
    - Test: fake-codex.mjs default mode emits 4 NDJSON lines (thread.started, turn.started, item.completed{agent_message,text:"pong"}, turn.completed{usage}) on stdout, exit 0
    - Test: fake-codex.mjs --fail-auth emits repeated error{message contains "401 Unauthorized"} + turn.failed on stdout, exit 1
    - Test: fake-codex.mjs --rate-limit emits turn.failed with message containing "429"/"RESOURCE_EXHAUSTED", exit 1
    - Test: fake-codex.mjs --bad-json writes "not json" to stdout exit 0; --hang never exits
    - Test: fake-gemini.mjs default writes {response:"pong",stats:{}} to STDOUT exit 0
    - Test: fake-gemini.mjs --fail-auth writes {session_id,error:{type:"Error",message:"Please set an Auth method...",code:41}} to STDERR exit 41
    - Test: fake-gemini.mjs --untrusted writes plain text "not running in a trusted directory" to STDERR exit 55; --rate-limit writes {error:{code:429,message:"RESOURCE_EXHAUSTED"}}; --bad-json; --hang
    - Test: CodexEvent.safeParse and GeminiJson.safeParse accept the fixture shapes and tolerate extra keys (.passthrough())
  </behavior>
  <action>
    Create `src/adapters/common.ts` exporting `splitBin` (move from claude.ts, re-export there for back-compat OR update claude.ts import), `safeJsonParse`, `redactArgv`, and `PROMPT_PLACEHOLDER = "<prompt>"` — extracted VERBATIM from claude.ts (do not re-derive). Update `src/adapters/claude.ts` to import these from common.ts instead of defining them inline; keep claude.ts behavior byte-identical (the existing claude-adapter.test.ts must still pass unchanged).
    Extend `src/schema/turn.ts` (do NOT touch TurnResult): add `export const CodexEvent = z.object({ type: z.string(), item: z.object({ type: z.string(), text: z.string().optional() }).partial().optional(), error: z.object({ message: z.string() }).partial().optional(), message: z.string().optional(), usage: z.unknown().optional() }).passthrough()` and `export const GeminiJson = z.object({ response: z.string().optional(), stats: z.unknown().optional(), session_id: z.string().optional(), error: z.object({ type: z.string().optional(), message: z.string(), code: z.number().optional() }).optional() }).passthrough()`, each with `export type` via z.infer. Keep the "MUST NOT leak past the adapter (D-12)" doc comment style.
    Create `test/fixtures/fake-codex.mjs` and `test/fixtures/fake-gemini.mjs` mirroring fake-claude.mjs structure (#!/usr/bin/env node, args=process.argv.slice(2), mode-select via args.includes, process.exit). Codex writes NDJSON lines via multiple process.stdout.write calls (each line a complete JSON + "\n"). Gemini failure modes use process.stderr.write per the LIVE-VERIFIED routing (D-32 / Pitfall 3). chmod +x both fixtures.
    Write RED tests asserting the schema parses + fixture shapes (a thin `test/registry.test.ts` placeholder may import not-yet-existing modules to stay RED). Commit `test(02-01): schemas + fixtures for codex/gemini adapters (RED)`.
  </action>
  <verify>
    <automated>npx vitest run test/codex-adapter.test.ts test/gemini-adapter.test.ts; test $? -ne 0 && echo RED-OK</automated>
  </verify>
  <acceptance_criteria>
    - `src/adapters/common.ts` exists exporting splitBin, safeJsonParse, redactArgv, PROMPT_PLACEHOLDER
    - `npx vitest run test/claude-adapter.test.ts` still passes (claude.ts refactor is behavior-preserving)
    - `grep -c 'passthrough' src/schema/turn.ts` returns >= 3 (ClaudeJson + CodexEvent + GeminiJson)
    - both fixtures exist and are executable (`test -x test/fixtures/fake-codex.mjs`)
    - codex/gemini adapter tests are RED (modules not yet implemented) — vitest exits non-zero
  </acceptance_criteria>
  <done>Shared common module + two new schemas + two fixtures exist; claude tests still green; new adapter tests RED.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement codex + gemini adapters (GREEN)</name>
  <read_first>
    - src/adapters/claude.ts (full skeleton to mirror: imports, execa call, timeout guard, TurnResult return shape)
    - src/adapters/common.ts (Task 1 — import splitBin/safeJsonParse/redactArgv from here)
    - src/schema/turn.ts (CodexEvent, GeminiJson from Task 1)
    - .planning/phases/02-adapter-layer-roster-pre-flight/02-PATTERNS.md (Pattern Assignments: codex.ts NDJSON parse loop, gemini.ts stdout-OR-stderr parse — copy the cited line refs)
    - .planning/phases/02-adapter-layer-roster-pre-flight/02-RESEARCH.md (codex ok-rule + gemini ok-rule + buildArgv arrays, LIVE-VERIFIED)
  </read_first>
  <behavior>
    - Test: codex happy -> ok:true, text:"pong" (from agent_message item.completed), exitCode 0, error undefined
    - Test: codex --fail-auth -> ok:false, text:"", error contains "401", timedOut:false
    - Test: codex --bad-json / no terminal event -> graceful ok:false error contains "unparseable", no throw
    - Test: codex --hang @ timeoutMs:200 -> ok:false, timedOut:true, error:"timeout"
    - Test: codex flag-pinning (execa mock) -> exact argv ["exec","--json","--skip-git-repo-check","--ephemeral","-s","read-only", prompt]; with model (makeCodexAdapter(bin,"o4")) -> ["...","-m","o4", prompt]; redactedCommand swaps prompt for "<prompt>"
    - Test: gemini happy -> ok:true, text:"pong" (top-level response), error undefined
    - Test: gemini --fail-auth (JSON on STDERR, exit 41) -> ok:false, error contains "Auth method", parsed from stderr not stdout
    - Test: gemini --untrusted (exit 55) -> ok:false, error contains "trusted directory"
    - Test: gemini flag-pinning -> exact argv ["-p", prompt, "--output-format","json","--skip-trust"]; with model (makeGeminiAdapter(bin,"flash")) -> ["...","-m","flash"]; .not.toContain("--yolo"); .not.toContain("-y")
  </behavior>
  <action>
    Create `src/adapters/codex.ts` exporting `makeCodexAdapter(bin = "codex", model?: string): AgentAdapter` (name:"codex"). The factory captures `model` in closure. buildArgv: `["exec","--json","--skip-git-repo-check","--ephemeral","-s","read-only"]`, push `["-m", model]` if model is set, then push promptText as the TRAILING positional. Mirror claude.ts execa opts + timeout guard verbatim. Parse `result.stdout.split("\n")` line-by-line with `CodexEvent.safeParse(safeJsonParse(line))`; track completed/failed/lastText/lastErr per PATTERNS Pattern 1. ok-rule: `result.exitCode === 0 && completed && !failed`. No parseable terminal event -> graceful ok:false with `error: \`unparseable output: ${result.stderr || "no json"}\``. Return TurnResult mirroring claude.ts shape (text: ok ? lastText : "", error: ok ? undefined : (lastErr || "codex error"), redactedCommand via redactArgv).
    Create `src/adapters/gemini.ts` exporting `makeGeminiAdapter(bin = "gemini", model?: string): AgentAdapter` (name:"gemini"). The factory captures `model` in closure. buildArgv: `["-p", promptText, "--output-format","json","--skip-trust"]`, push `["-m", model]` if model is set. CRITICAL: `const parsed = GeminiJson.safeParse(safeJsonParse(result.stdout) ?? safeJsonParse(result.stderr))` (stdout-OR-stderr — D-32 / Pitfall 3). Unparseable -> graceful ok:false. ok-rule: `result.exitCode === 0 && j.error == null && typeof j.response === "string"`. text: ok ? j.response : "". error: ok ? undefined : (j.error?.message ?? result.stderr ?? "gemini error"). Do NOT allowlist exit codes (undocumented 41/55).
    MODEL CONTRACT (PINNED — no deferral): both factories take `model?` as the SECOND param and capture it in closure; buildArgv appends `-m <model>` (codex/gemini) when set. The roster supplies `entry.model` and the CLI threads it via `makeAdapter(vendor, bin, model)` in Plan 05 — the adapters accept it now via `make*Adapter(bin?, model?)`. TurnRequest is NOT extended; model is a factory-closure param, not a per-request field.
    Write GREEN implementations to pass Task 1 behaviors. Commit `feat(02-01): codex + gemini adapters (GREEN)`.
  </action>
  <verify>
    <automated>npx vitest run test/codex-adapter.test.ts test/gemini-adapter.test.ts --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - codex flag-pinning test asserts EXACT argv including --skip-git-repo-check, --ephemeral, -s read-only; with-model variant asserts `-m <model>` present
    - gemini flag-pinning test asserts --skip-trust present and --yolo absent; with-model variant asserts `-m <model>` present
    - gemini --fail-auth test proves the error JSON is parsed from STDERR (not stdout)
    - both adapters return camelCase TurnResult; `grep -E 'turn\.completed|is_error|response' src/adapters/registry.ts` finds nothing (no vendor field names leak past adapters)
    - all codex/gemini adapter tests green
  </acceptance_criteria>
  <done>Both adapters normalize their LIVE-VERIFIED happy + failure shapes into TurnResult; flag-pinning tests pin exact argv (incl. the model flag); all tests green.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Vendor->adapter registry (the ORCH-03 seam)</name>
  <read_first>
    - src/adapters/claude.ts (makeClaudeAdapter signature)
    - src/adapters/codex.ts, src/adapters/gemini.ts (Task 2 signatures)
    - .planning/phases/02-adapter-layer-roster-pre-flight/02-PATTERNS.md (registry.ts pattern — FACTORIES map)
  </read_first>
  <behavior>
    - Test: makeAdapter("claude") returns an adapter with name "claude"; same for codex, gemini
    - Test: makeAdapter("codex", "/custom/bin") passes the bin override through to the factory
    - Test: makeAdapter("codex", "/custom/bin", "o4") threads BOTH bin and model through to the factory (model flag appears in the spawned argv)
    - Test: the FACTORIES map has exactly the three vendor keys; adding a vendor is a one-line map entry (assert keys === ["claude","codex","gemini"])
    - Test: a TypeScript-invalid vendor is rejected at the type boundary (vendor param is keyof typeof FACTORIES)
  </behavior>
  <action>
    Create `src/adapters/registry.ts`: import the three factories, `const FACTORIES = { claude: makeClaudeAdapter, codex: makeCodexAdapter, gemini: makeGeminiAdapter } as const;` and `export function makeAdapter(vendor: keyof typeof FACTORIES, bin?: string, model?: string): AgentAdapter { return FACTORIES[vendor](bin, model); }`. This is the AUTHORITATIVE model-param signature — bin and model both thread through to each factory's closure. Each factory defaults its own bin. This is the ORCH-03 seam: adding a vendor = adding one FACTORIES entry, zero protocol change. Convert `test/registry.test.ts` placeholder into the real GREEN test. Commit `feat(02-01): vendor->adapter registry (ORCH-03 seam)`.
  </action>
  <verify>
    <automated>npx vitest run test/registry.test.ts test/codex-adapter.test.ts test/gemini-adapter.test.ts test/claude-adapter.test.ts --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - `makeAdapter` returns correct-named adapters for all three vendors
    - bin AND model overrides thread through to the factory (signature `makeAdapter(vendor, bin?, model?)`)
    - registry test asserts FACTORIES keys are exactly claude/codex/gemini
    - full adapter+registry test set green; `npx tsc --noEmit` clean
  </acceptance_criteria>
  <done>One registry function selects the adapter by vendor literal; tests prove all three resolve and the bin+model overrides thread through.</done>
</task>

</tasks>

## Tasks — CONTEXT.md decision citations

- **D-32:** Gemini adapter is FIXTURE-BUILT (real gemini auth broken); built/tested entirely against `fake-gemini.mjs`; error JSON parsed from stdout-OR-stderr; no gemini exit-code allowlist. (Tasks 1, 2)
- **D-35:** No new dependencies — codex NDJSON parsing via `String.split("\n")`, gemini JSON via existing zod; only execa/zod/node-builtins. (Tasks 1, 2, 3)

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| config-supplied `bin`/`model` -> execa argv | values from mar.config.json become argv elements (Plan 03 supplies; adapters consume) |
| CLI stdout/stderr -> adapter parse | untrusted subprocess output parsed by adapters |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-01 | Tampering | codex/gemini argv build | mitigate | execa array args (no shell); prompt + model are argv elements, never interpolated into a shell string (mirror T-01-05) |
| T-02-02 | Tampering | adapter parse of CLI output | mitigate | zod `.safeParse` on every NDJSON line + gemini JSON; unparseable -> graceful ok:false, never eval/exec of output |
| T-02-03 | Info Disclosure | redactedCommand / logs | mitigate | redactArgv swaps prompt body for "<prompt>" using the SAME spawned array; prompt body never in redactedCommand |
| T-02-04 | Elevation of Privilege | codex sandbox | mitigate | adapter pins `-s read-only`; never `--dangerously-bypass-approvals-and-sandbox` |
| T-02-05 | Tampering | gemini trusted-dir gate | accept | `--skip-trust` pinned in argv (the run dir is the user's own project); exit-55 surfaced as a distinct error, not silently bypassed |
| T-02-SC | Tampering | npm installs | mitigate | none added this phase (D-35); RESEARCH Package Legitimacy Audit = N/A |
</threat_model>

<verification>
- `npx vitest run` — full suite green (Phase 1's tests + all Plan 01 adapter/registry tests)
- `npx tsc --noEmit` clean; `npx biome check src/adapters src/schema test/codex-adapter.test.ts test/gemini-adapter.test.ts test/registry.test.ts` clean
- `grep -REn 'turn\.completed|item\.completed|is_error|"response"' src/adapters/registry.ts` empty (no vendor field names leak past adapter tier — D-12)
- both fixtures executable; gemini fixture writes failure JSON to stderr
</verification>

<success_criteria>
- makeAdapter(vendor, bin?, model?) returns claude|codex|gemini adapters through one uniform AgentAdapter contract (ORCH-03 adapter seam)
- codex + gemini adapters pass happy + all failure-mode + flag-pinning tests (incl. the model flag) against fixtures
- TurnResult contract unchanged; no vendor field names past the adapter boundary
- zero real CLI credits spent (all fixture-driven)
</success_criteria>

<output>
Create `.planning/phases/02-adapter-layer-roster-pre-flight/02-01-SUMMARY.md` when done
</output>
