---
phase: 02-adapter-layer-roster-pre-flight
plan: 05
type: execute
wave: 3
depends_on: ["02-01", "02-02", "02-03", "02-04"]
files_modified:
  - src/cli.ts
  - test/e2e-invoke.test.ts
  - test/cli-roster.test.ts
autonomous: false
requirements: [ORCH-02, ORCH-03, ORCH-05]
user_setup:
  - service: codex
    why: "Live human-verify checkpoint invokes the real codex CLI (claude already verified Phase 1)"
    env_vars: []
    dashboard_config:
      - task: "codex authenticated via ChatGPT account (already true on this machine per RESEARCH)"
        location: "codex login"
must_haves:
  truths:
    - "mar init writes a starter mar.config.json; mar preflight prints the per-agent status table and exits 0/1; mar invoke --agent <name> resolves by roster name and drives the right vendor adapter through withRetry"
    - "mar invoke is EXEMPT from the >=2-vendor gate and does NOT auto-preflight; every attempt is logged with its attempt number"
    - "the existing e2e-invoke test stays green after invoke switches from hardcoded claude to roster-name resolution"
    - "a live mar init -> mar preflight -> mar invoke --agent codex-1 round-trip is human-verified (claude+codex responsive, gemini ✗ with hint)"
  artifacts:
    - path: "src/cli.ts"
      provides: "init + preflight subcommands; roster-resolved invoke wrapped in withRetry"
      contains: "preflight"
  key_links:
    - from: "src/cli.ts invoke"
      to: "src/config.ts resolveAgent + src/adapters/registry.ts makeAdapter"
      via: "loadConfig -> resolveAgent -> makeAdapter(entry.vendor, entry.bin)"
      pattern: "resolveAgent|makeAdapter"
    - from: "src/cli.ts invoke"
      to: "src/retry.ts withRetry"
      via: "withRetry wrapping adapter.invoke, onAttempt -> logInvocation"
      pattern: "withRetry"
---

<objective>
Wire the roster machinery into the CLI — the user-observable vertical slice that ties Plans 01-04 together: `mar init` (write starter roster), `mar preflight` (status table + exit code), and a roster-resolved `mar invoke` that wraps the adapter in `withRetry` and logs every attempt. Keep the CLI thin — business logic stays in config/preflight/gates/retry so Phase 3's `mar run` reuses them. End with a live human-verify checkpoint mirroring Phase 1's pattern.

Purpose: closes ORCH-02 (retry now wraps real invocations), ORCH-03 (invoke resolves by roster name through the registry), and ORCH-05 (mar preflight is runnable). This is the slice where a user can actually configure a roster and see CLI problems surfaced before a run.

Output: extended `src/cli.ts` (init + preflight subcommands; invoke roster-resolution + withRetry), updated `test/e2e-invoke.test.ts`, new `test/cli-roster.test.ts`. Final task is a blocking live checkpoint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/02-adapter-layer-roster-pre-flight/02-CONTEXT.md
@.planning/phases/02-adapter-layer-roster-pre-flight/02-PATTERNS.md
@.planning/phases/01-workspace-first-adapter/01-03-SUMMARY.md

<interfaces>
<!-- The CLI composes everything built in Plans 01-04. Use directly. -->

From src/config.ts (Plan 03):  loadConfig(path?) ; resolveAgent(config, name): AgentEntry
From src/adapters/registry.ts (Plan 01):  makeAdapter(vendor, bin?, model?): AgentAdapter
From src/retry.ts (Plan 02):  withRetry(invoke, { retries, classify, onAttempt, ... }) ; classify{Claude,Codex,Gemini}
From src/init.ts (Plan 03):  detectVendors() ; writeStarterConfig(path, vendors)
From src/preflight.ts (Plan 04):  runPreflight(roster, opts?) -> { results, allPass } ; formatStatusLines(results)
From src/log/invocation.ts (Plan 02):  logInvocation(runDir, record)  // record now carries `attempt`

Existing src/cli.ts (Plan 01-03) seams to MODIFY:
- runInvoke: REPLACE `if (opts.agent !== "claude")` guard (lines 92-97) + `MAR_CLAUDE_BIN` default (line 99) with loadConfig -> resolveAgent -> makeAdapter
- detectClaudeVersion (lines 74-88): generalize OR reuse preflight.extractVersion for the per-vendor case
- buildProgram (lines 230-247): add `.command("init")` and `.command("preflight")` mirroring the invoke chain
- KEEP: turn.ok-only branching (line 176 / T-01-13), one-line console (lines 218-225), RUN_ID_RE charset guard
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Roster-resolved invoke + withRetry + init/preflight subcommands (RED then GREEN)</name>
  <read_first>
    - src/cli.ts (FULL file — the exact seams to modify: runInvoke agent guard, bin default, buildProgram)
    - src/config.ts, src/adapters/registry.ts, src/retry.ts, src/init.ts, src/preflight.ts (the composables from Plans 01-04)
    - src/log/invocation.ts (logInvocation with the new attempt field)
    - test/e2e-invoke.test.ts (the existing green e2e — must stay green after roster resolution; it injects the fixture)
    - .planning/phases/02-adapter-layer-roster-pre-flight/02-PATTERNS.md (cli.ts assignment — keep thin, preserve T-01-13 + one-line console)
  </read_first>
  <behavior>
    - Test: `mar invoke --agent codex-1` against a roster whose codex-1 entry has bin pointing at fake-codex.mjs -> resolves the codex adapter, writes artifact, status completed, exit 0 (no hardcoded "claude")
    - Test: `mar invoke --agent nope` -> clear error listing valid agent names, exit 2
    - Test: `mar invoke` with NO mar.config.json -> "no roster" error pointing at `mar init`, exit 2
    - Test: invoke is EXEMPT from the >=2-vendor gate (a single-vendor roster still invokes) and does NOT call runPreflight (D-27/D-29)
    - Test: a transient-then-ok adapter (fake-codex --rate-limit handling) is RETRIED via withRetry; BOTH attempts are logged to invocations.ndjson with attempt 1 and 2 (D-25) — use near-zero backoff injection so the test is fast
    - Test: `mar init` writes a starter mar.config.json (PATH injected to a temp dir with stub bins) that re-parses through MarConfig
    - Test: `mar preflight` against a fixture-bin roster prints the status lines and sets exit code 0 (all pass) or 1 (any fail); writes the cache
    - Test: the existing e2e-invoke.test.ts (claude slice) stays GREEN after switching invoke to roster resolution (provide a mar.config.json or roster injection it can use)
  </behavior>
  <action>
    Modify `src/cli.ts`:
    - `invoke` (D-20): REMOVE the `opts.agent !== "claude"` guard and the `MAR_CLAUDE_BIN` hardcode. Instead `loadConfig()` -> `resolveAgent(config, opts.agent)` -> `makeAdapter(entry.vendor, entry.bin, entry.model)`. Resolve effective timeout (`entry.timeoutMs ?? config.defaults.timeoutMs`) and retries (`config.defaults.retries`). Wrap the adapter call in `withRetry(() => adapter.invoke({...}), { retries, classify: classifyFor(entry.vendor), onAttempt: (turn, attempt) => logInvocation(runDir, { command: turn.redactedCommand, promptRef, exitCode: turn.exitCode, durationMs: turn.durationMs, timedOut: turn.timedOut, artifactPath, attempt }) })`. Keep persistence branching ONLY on the FINAL turn.ok (T-01-13). `mar invoke` stays EXEMPT from the gate and does NOT auto-preflight (D-27/D-29). Use `extractVersion` (Plan 04) for the per-vendor cliVersions capture at run-create (fixes the codex "codex-cli" bug).
    - Add `program.command("init")`: `detectVendors()` -> `writeStarterConfig("mar.config.json", vendors)`; print a one-line summary of detected vendors. (D-21)
    - Add `program.command("preflight")` with an optional `--skip-failed` note for Phase-3 parity (the flag's run-start behavior lives in gates.ts; here preflight just reports): `loadConfig()` -> `runPreflight(config.agents)` -> print `formatStatusLines(results)` -> `process.exitCode = allPass ? 0 : 1`. (D-28)
    - Keep the bin-entry guard, RUN_ID_RE, resolvePrompt, parseTimeout unchanged.
    Write RED tests (`test/cli-roster.test.ts` for the new commands + the retry-logging-through-CLI case; update `test/e2e-invoke.test.ts` to supply a roster) then GREEN. Inject fixture bins via the roster `bin` field (D-19) — NOT a new env var. Commit `test(02-05): roster-resolved invoke + init/preflight (RED)` then `feat(02-05): wire init/preflight + roster invoke + withRetry (GREEN)`.
  </action>
  <verify>
    <automated>npx vitest run test/cli-roster.test.ts test/e2e-invoke.test.ts --reporter=dot</automated>
  </verify>
  <acceptance_criteria>
    - `mar invoke --agent codex-1` drives the codex adapter via roster resolution (no hardcoded vendor); unknown name + missing roster give exit-2 errors
    - invoke wraps the adapter in withRetry and logs EVERY attempt with its attempt number (assert 2 records on a retried call)
    - `mar invoke` neither enforces the >=2-vendor gate nor calls runPreflight
    - `mar init` and `mar preflight` subcommands exist and behave per behavior list
    - existing e2e-invoke.test.ts stays green; `npx vitest run` full suite green; `npx tsc --noEmit` clean
    - `grep -c 'opts.agent !== "claude"' src/cli.ts` == 0 (hardcoded vendor guard removed)
  </acceptance_criteria>
  <done>The CLI exposes init/preflight and a roster-resolved, retry-wrapped invoke; all attempts logged; full suite green with zero real credits.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Live mar init -> preflight -> invoke round-trip (human-verify)</name>
  <what-built>
    A roster-driven CLI: `mar init` writes a starter mar.config.json from PATH detection; `mar preflight` checks each roster CLI (installed/authenticated/responsive) and prints a status table; `mar invoke --agent <name>` resolves by roster name and drives the matching vendor adapter wrapped in bounded retry. All automated tests pass against fixtures (zero credits). This checkpoint verifies the real CLIs end-to-end, mirroring Phase 1's live claude smoke.
  </what-built>
  <how-to-verify>
    Run on the user's machine (CI cannot — burns real credits, needs interactive auth):
    1. `npx tsx src/cli.ts init` — expect a `mar.config.json` written listing detected vendors (claude, codex, and likely gemini). Inspect the file: structured agents + defaults{timeoutMs,retries:2}.
    2. `npx tsx src/cli.ts preflight` — EXPECT: `claude-1` ✓ installed (2.1.x) ✓ responsive; `codex-1` ✓ installed (0.128.0) ✓ responsive; `gemini-1` ✓ installed (0.45.0) ✗ responsive with an auth/Antigravity HINT (this is CORRECT per D-32 — gemini headless auth is broken on this machine). Exit code 1 (any-fail). Confirm `.mar/preflight.json` was written and `.mar/` is gitignored.
    3. `npx tsx src/cli.ts invoke --agent codex-1 --prompt "Reply with exactly: pong"` — EXPECT one human-readable progress line `codex ✓  <s>s  exit 0  → runs/<id>/001-codex-output.md`, a normalized artifact + raw sibling, manifest status completed with cliVersions.codex = "0.128.0" (NOT "codex-cli"), and an invocations.ndjson record carrying an `attempt` field. Confirm the prompt body is NOT in the log (promptRef only).
    4. (Optional) `npx tsx src/cli.ts invoke --agent claude-1 --prompt "Reply with exactly: pong"` — expect claude ✓ (re-confirms Phase-1 path through the new roster resolution).
  </how-to-verify>
  <resume-signal>Type "approved" once preflight shows claude/codex responsive + gemini ✗-with-hint and a codex invoke produced an artifact, or describe issues.</resume-signal>
</task>

</tasks>

## Tasks — CONTEXT.md decision citations

- **D-20:** `mar invoke --agent <X>` resolves against roster entry NAMES only; missing roster file -> clear error; single resolution path through resolveAgent. (Task 1)
- **D-21:** `mar init` probes PATH and writes a starter `mar.config.json` with detected vendors. (Task 1)
- **D-24:** Retry logic wraps `AgentAdapter.invoke` in ONE vendor-agnostic wrapper (`withRetry`) at the CLI call site — not duplicated per adapter. (Task 1)
- **D-25:** Every attempt (incl. failures) gets its own `invocations.ndjson` record with an attempt number via the withRetry onAttempt callback. (Task 1)
- **D-27:** `mar preflight` is the explicit trigger; `mar invoke` does NOT auto-preflight (run-start auto-preflight + cache consumption is Phase 3). (Task 1)
- **D-28:** `mar preflight` prints the per-agent status table and exits 0 (all pass) / 1 (any fail). (Task 1)
- **D-29:** `mar invoke` (single invocation, not a review run) is EXEMPT from the >=2-distinct-vendor gate. (Task 1)
- **D-34:** `mar init` writes `mar.config.json` as a committed project file (not gitignored). (Task 1)

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| `--agent` arg + mar.config.json -> adapter selection + argv | user/config input selects vendor + bin + extraArgs |
| `--prompt` + roster fields -> live CLI invocation | crosses into a real subprocess at the checkpoint |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-17 | Tampering | --run / --agent / config args | mitigate | RUN_ID_RE charset guard retained; agent resolved against the validated roster only; adapter argv stays an execa array (no shell — inherits T-01-05/T-02-01) |
| T-02-18 | Info Disclosure | console + invocation log | mitigate | one human-readable line only; promptRef is a reference, never the body; redactedCommand from the adapter; attempt records carry no prompt body or creds (D-15/D-25) |
| T-02-19 | Spoofing | success re-derivation at CLI | mitigate | persistence branches ONLY on the final `turn.ok`; exit code/subtype never re-derived (T-01-13) |
| T-02-20 | Denial of Service / $$ | retry at the live call site | mitigate | withRetry bounded by config.defaults.retries; fatal classification stops auth loops; per-attempt timeout bounds each call |
| T-02-SC | Tampering | npm installs | mitigate | none added this phase (D-35); legitimacy gate N/A |
</threat_model>

<verification>
- `npx vitest run` — FULL suite green (Phase 1 + all Phase 2 plans)
- `npx tsc --noEmit` clean; `npx biome check src/cli.ts test/cli-roster.test.ts test/e2e-invoke.test.ts` clean
- `grep -c 'opts.agent !== "claude"' src/cli.ts` == 0; `grep -c 'withRetry' src/cli.ts` >= 1
- Live checkpoint approved (claude/codex responsive, gemini ✗-with-hint, codex invoke produced an artifact + attempt-logged record)
</verification>

<success_criteria>
- A user can `mar init`, `mar preflight`, and `mar invoke --agent <name>` against a multi-vendor roster (ORCH-03/05)
- Every invocation is retry-wrapped and every attempt is logged (ORCH-02/D-25)
- gemini correctly preflights ✗ with a hint while claude+codex satisfy the working pair (D-32)
- Live round-trip human-verified
</success_criteria>

<output>
Create `.planning/phases/02-adapter-layer-roster-pre-flight/02-05-SUMMARY.md` when done
</output>
